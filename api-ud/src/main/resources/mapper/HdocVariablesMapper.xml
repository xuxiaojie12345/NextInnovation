<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocVariablesMapper">
    
    <resultMap id="HdocVariablesResultMap" type="com.web.app.entity.HdocVariables">
        <id property="variable" column="VARIABLE"/>
        <result property="type" column="TYPE"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="createdByUser" column="REGISTER_USER"/>
        <result property="createDate" column="REGISTER_DATETIME"/>
    </resultMap>
    
    <!-- 根据变量名计数 -->
    <select id="countByVariable" resultType="int">
        SELECT COUNT(1) FROM HDOC_VARIABLES WHERE VARIABLE = #{variable}
    </select>
    
    <!-- 插入变量 -->
    <insert id="insert">
        INSERT INTO HDOC_VARIABLES (
            VARIABLE, TYPE, DESCRIPTION,
            REGISTER_USER, REGISTER_DATETIME, REGISTER_PROCESS,
            UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS
        ) VALUES (
            #{variable}, #{type}, #{description},
            #{createdByUser}, SYSDATE(), 'UD10Add',
            SYSDATE(), #{createdByUser}, 'UD10Add'
        )
    </insert>
    
    <!-- 更新变量 -->
    <update id="update">
        UPDATE HDOC_VARIABLES
        SET TYPE = #{type},
            DESCRIPTION = #{description},
            UPDATE_USER = #{createdByUser},
            UPDATE_DATETIME = SYSDATE()
        WHERE VARIABLE = #{variable}
    </update>
    
    <!-- 删除变量 -->
    <delete id="deleteByVariable">
        DELETE FROM HDOC_VARIABLES WHERE VARIABLE = #{variable}
    </delete>
    
    <!-- UD08 - 查询所有变量名（仅VARIABLE字段） -->
    <select id="selectAllVariables" resultMap="HdocVariablesResultMap">
        SELECT VARIABLE FROM HDOC_VARIABLES
    </select>

    <!-- 搜索变量 -->
    <select id="searchVariables" resultMap="HdocVariablesResultMap">
        SELECT VARIABLE, TYPE, DESCRIPTION, REGISTER_USER, REGISTER_DATETIME
        FROM HDOC_VARIABLES
        WHERE 1=1
        <if test="variable != null and variable != ''">
            AND VARIABLE LIKE CONCAT('%', #{variable}, '%')
        </if>
        <if test="type != null and type != ''">
            AND TYPE LIKE CONCAT('%', #{type}, '%')
        </if>
        <if test="description != null and description != ''">
            AND DESCRIPTION LIKE CONCAT('%', #{description}, '%')
        </if>
    </select>
    
</mapper>
