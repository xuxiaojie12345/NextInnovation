<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocVariablesMapper">

    <!-- UD08/UD10/UD11: 查询Variable -->
    <select id="selectByVariable" resultType="com.web.app.entity.HdocVariables">
        SELECT VARIABLE, TYPE, DESCRIPTION, USERID,
               REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
               UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS
        FROM HDOC_VARIABLES
        WHERE VARIABLE = #{variable}
    </select>

    <!-- UD10: 更新 -->
    <update id="updateByVariable">
        UPDATE HDOC_VARIABLES
        SET
            TYPE = #{type},
            DESCRIPTION = #{description},
            UPDATE_DATETIME = #{updateDatetime},
            UPDATE_USER = #{updateUser},
            UPDATE_PROCESS = #{updateProcess}
        WHERE VARIABLE = #{variable}
    </update>

    <!-- UD10: 新增 -->
    <insert id="insertVariable">
        INSERT INTO HDOC_VARIABLES
        (VARIABLE, TYPE, DESCRIPTION,
         REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
         UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS)
        VALUES
        (#{variable}, #{type}, #{description},
         #{registerDatetime}, #{registerUser}, #{registerProcess},
         #{updateDatetime}, #{updateUser}, #{updateProcess})
    </insert>

    <!-- UD10: 删除 -->
    <delete id="deleteByVariable">
        DELETE FROM HDOC_VARIABLES
        WHERE VARIABLE = #{variable}
    </delete>

    <!-- UD11: 模糊检索 -->
    <select id="searchVariables" resultType="com.web.app.dto.HdocVariablesResponse">
        SELECT VARIABLE, TYPE, DESCRIPTION,
               REGISTER_DATETIME AS registerDatetime,
               REGISTER_USER AS registerUser,
               REGISTER_PROCESS AS registerProcess
        FROM HDOC_VARIABLES
        <where>
            <if test="variable != null and variable != ''">AND VARIABLE LIKE CONCAT('%', #{variable}, '%')</if>
            <if test="description != null and description != ''">AND DESCRIPTION LIKE CONCAT('%', #{description}, '%')</if>
        </where>
    </select>

    <!-- UD11: 统计 -->
    <select id="countVariables" resultType="int">
        SELECT COUNT(VARIABLE)
        FROM HDOC_VARIABLES
        <where>
            <if test="variable != null and variable != ''">AND VARIABLE LIKE CONCAT('%', #{variable}, '%')</if>
            <if test="description != null and description != ''">AND DESCRIPTION LIKE CONCAT('%', #{description}, '%')</if>
        </where>
    </select>

</mapper>
