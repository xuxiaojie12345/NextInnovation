<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocUserDocMapper">

    <!-- UD18: 查询用户文档权限 -->
    <select id="selectByUserId" resultType="com.web.app.entity.HdocUserDoc">
        SELECT DOCTYPE FROM HDOC_USER_DOC WHERE USERID = #{userId}
    </select>

    <!-- UD18: 删除该用户所有权限 -->
    <delete id="deleteByUserId">
        DELETE FROM HDOC_USER_DOC WHERE USERID = #{userId}
    </delete>

    <!-- UD18: 新增 -->
    <insert id="insertUserDoc">
        INSERT INTO HDOC_USER_DOC
        (USERID, DOCTYPE, REGISTER_USER, REGISTER_DATETIME, REGISTER_PROCESS,
         UPDATE_USER, UPDATE_DATETIME, UPDATE_PROCESS)
        VALUES
        (#{userid}, #{doctype}, #{registerUser}, #{registerDatetime}, #{registerProcess},
         #{updateUser}, #{updateDatetime}, #{updateProcess})
    </insert>

</mapper>
