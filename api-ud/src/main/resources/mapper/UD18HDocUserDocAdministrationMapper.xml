<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UD18HDocUserDocAdministrationMapper">

    <!--
        UD18 5.36 - 查询语句1: 检查HDOC_FUNCTION_AUTH是否存在该用户记录
    -->
    <select id="countFunctionAuthByUserId" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM HDOC_FUNCTION_AUTH
        WHERE USERID = #{userId}
    </select>

    <!--
        UD18 5.37 - 查询语句2: 查询用户文档类型（doctype为可选参数）
        - 当doctype不为空时，精确匹配文档类型
        - 当doctype为空时，返回该用户所有文档类型
    -->
    <select id="selectUserDocByUserId" resultType="java.util.Map">
        SELECT DOCTYPE AS doctype
        FROM HDOC_USER_DOC
        WHERE USERID = #{userId}
        <if test="doctype != null and doctype != ''">
            AND DOCTYPE = #{doctype}
        </if>
    </select>

    <!--
        UD18 5.38.1 - 插入语句: 插入用户文档权限到HDOC_USER_DOC表
    -->
    <insert id="insertUserDoc">
        INSERT INTO HDOC_USER_DOC
            (USERID, DOCTYPE, REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
             UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS)
        VALUES
            (#{userId}, #{doctype}, NOW(), #{registerUser}, #{registerProcess},
             NOW(), #{updateUser}, #{updateProcess})
    </insert>

    <!--
        UD18 5.38.2 - 删除语句: 删除用户文档权限
    -->
    <delete id="deleteUserDoc">
        DELETE FROM HDOC_USER_DOC
        WHERE USERID = #{userId} AND DOCTYPE = #{doctype}
    </delete>

</mapper>
