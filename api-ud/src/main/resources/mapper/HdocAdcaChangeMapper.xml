<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocAdcaChangeMapper">
    <select id="selectBySerieAndChnr" resultType="com.web.app.entity.HdocAdcaChange">
        SELECT * FROM HDOC_ADCA_CHANGE WHERE SERIE = #{serie} AND CHNR = #{chnr}
    </select>
    <insert id="insert" parameterType="com.web.app.entity.HdocAdcaChange">
        INSERT INTO HDOC_ADCA_CHANGE
          (SERIE, CHNR, ACT, BU, REASON,
           REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
           UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS)
        VALUES
          (#{serie}, #{chnr}, #{act}, #{bu}, #{reason},
           NOW(), #{registerUser}, #{registerProcess},
           NOW(), #{updateUser}, #{updateProcess})
    </insert>
    <update id="updateActToN">
        UPDATE HDOC_ADCA_CHANGE SET ACT = 'N' WHERE SERIE = #{serie} AND CHNR = #{chnr}
    </update>
</mapper>
