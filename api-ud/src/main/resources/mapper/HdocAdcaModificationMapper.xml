<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocAdcaModificationMapper">
    
    <!-- 根据Chassis series和Chassis no查询Variant信息 -->
    <select id="selectVariantInfo" resultType="com.web.app.domain.UD05ModifyDocumentResponse$VariableItem">
        SELECT
            b.VARIABLE AS variable,
            b.DESCRIPTION AS description,
            a.NEWVAL AS newval,
            a.NEWVAL AS currentValue
        FROM
            HDOC_ADCA_MODIFICATION a
        LEFT JOIN
            HDOC_VARIABLES b
        ON
            a.VARIABLE = b.VARIABLE
        WHERE
            a.SERIE = #{chassisSeries}
            AND a.CHNO = #{chassisNo}
    </select>
    
    <!-- 更新HDOC_ADCA_MODIFICATION表的NEWVAL字段 -->
    <update id="updateNewval">
        UPDATE HDOC_ADCA_MODIFICATION
        SET 
            NEWVAL = #{newval},
            UPDATE_USER = #{updateUser},
            UPDATE_DATETIME = NOW()
        WHERE 
            SERIE = #{chassisSeries}
            AND CHNO = #{chassisNo}
            AND VARIABLE = #{variable}
    </update>
    
</mapper>
