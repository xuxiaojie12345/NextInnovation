<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UserInfoMapper">
    <select id="selectUserById" resultType="com.web.app.domain.Entity.UserInfo">
        SELECT
            USERID, PASSWORD, USERNAME, RESPONSIBLE, USERPOSITION,
            "E-MMAIL" AS eMmail, REGISTER_DATETIME, REGISTER_USER,
            REGISTER_PROCESS, UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS
        FROM hdoc_user_infor
        WHERE USERID = #{userId}
    </select>
</mapper>