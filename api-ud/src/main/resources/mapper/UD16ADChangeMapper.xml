<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UD16ADChangeMapper">

    <!--
        UD16 根据系列和底盘号查询AD/CA变更记录
    -->
    <select id="selectAdcaChange" resultType="com.web.app.entity.HdocAdcaChange">
        SELECT *
        FROM HDOC_ADCA_CHANGE
        WHERE SERIE = #{serie}
          AND CHNR = #{chnr}
    </select>

    <!--
        UD16 插入AD/CA变更记录
    -->
    <insert id="insertAdcaChange" parameterType="com.web.app.entity.HdocAdcaChange">
        INSERT INTO HDOC_ADCA_CHANGE (
            SERIE, CHNR, ACT, BU, REASON,
            REGISTER_USER, REGISTER_PROCESS,
            UPDATE_USER, UPDATE_PROCESS,
            REGISTER_DATETIME, UPDATE_DATETIME
        ) VALUES (
            #{serie}, #{chnr}, 'Y', 'UD', #{reason},
            #{registerUser}, #{registerProcess},
            #{updateUser}, #{updateProcess},
            NOW(), NOW()
        )
    </insert>

    <!--
        UD16 DELETE: 物理删除AD/CA变更记录
    -->
    <delete id="deleteAdcaChange">
        DELETE FROM HDOC_ADCA_CHANGE
        WHERE SERIE = #{serie}
          AND CHNR = #{chnr}
    </delete>

</mapper>
