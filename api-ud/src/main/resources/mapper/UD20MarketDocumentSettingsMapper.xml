<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UD20MarketDocumentSettingsMapper">

    <!--
        UD20-1 根据 DOCTYPE 统计记录数
        用于检查指定文档类型是否存在
    -->
    <select id="countByDoctype" resultType="int">
        SELECT COUNT(1)
        FROM HDOC_DOCUMENT_LIST
        WHERE DOCTYPE = #{doctype}
    </select>

    <!--
        UD20-1 更新文档列表
        功能说明：根据 DOCTYPE 更新 HDOC_DOCUMENT_LIST 表
        - 更新 REGISTER_USER 和 REGISTER_DATETIME 字段
    -->
    <update id="updateDocumentList">
        UPDATE HDOC_DOCUMENT_LIST
        SET
            REGISTER_USER = #{registerUser},
            REGISTER_DATETIME = #{date},
            UPDATE_USER = #{user},
            UPDATE_DATETIME = #{date},
            UPDATE_PROCESS = #{process}
        WHERE DOCTYPE = #{doctype}
    </update>

</mapper>
