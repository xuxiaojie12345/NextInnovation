<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.HdocUserDefinedRulesMapper">
    
    <resultMap id="HdocUserDefinedRulesResultMap" type="com.web.app.entity.HdocUserDefinedRules">
        <id property="pc" column="PC"/>
        <result property="num" column="NUM"/>
        <result property="market" column="MARKET"/>
        <result property="variable" column="VARIABLE"/>
        <result property="val" column="VAL"/>
        <result property="vs" column="VS"/>
        <result property="vs2" column="VS2"/>
        <result property="comments" column="COMMENTS"/>
        <result property="addDate" column="ADD_DATE"/>
        <result property="deleteDate" column="DELETE_DATE"/>
        <result property="registerUser" column="REGISTER_USER"/>
        <result property="registerDatetime" column="REGISTER_DATETIME"/>
    </resultMap>
    
    <!-- 根据条件计数 -->
    <select id="countByCondition" resultType="int">
        SELECT COUNT(1) FROM HDOC_USER_DEFINED_RULES 
        WHERE PC = #{pc} AND NUM = #{num} AND MARKET = #{market}
    </select>
    
    <!-- 插入规则 -->
    <insert id="insert">
        INSERT INTO HDOC_USER_DEFINED_RULES (
            PC, NUM, MARKET, VARIABLE, VAL, VS, VS2, COMMENTS, 
            ADD_DATE, DELETE_DATE, REGISTER_USER, REGISTER_DATETIME
        ) VALUES (
            #{pc}, #{num}, #{market}, #{variable}, #{val}, #{vs}, #{vs2}, #{comments},
            #{addDate}, #{deleteDate}, #{registerUser}, #{registerDatetime}
        )
    </insert>
    
    <!-- 更新规则 -->
    <update id="update">
        UPDATE HDOC_USER_DEFINED_RULES
        SET VARIABLE = #{variable},
            VAL = #{val},
            VS = #{vs},
            VS2 = #{vs2},
            COMMENTS = #{comments},
            ADD_DATE = #{addDate},
            DELETE_DATE = #{deleteDate},
            UPDATE_USER = #{registerUser},
            UPDATE_DATETIME = #{registerDatetime}
        WHERE PC = #{pc} AND NUM = #{num} AND MARKET = #{market}
    </update>
    
    <!-- 删除规则 -->
    <delete id="deleteByCondition">
        DELETE FROM HDOC_USER_DEFINED_RULES
        WHERE PC = #{pc} AND NUM = #{num} AND MARKET = #{market}
    </delete>
    
    <!-- 搜索规则 -->
    <select id="searchRules" resultMap="HdocUserDefinedRulesResultMap">
        SELECT PC, NUM, MARKET, VARIABLE, VAL, VS, VS2, COMMENTS, 
               ADD_DATE, DELETE_DATE, REGISTER_USER, REGISTER_DATETIME
        FROM HDOC_USER_DEFINED_RULES
        WHERE 1=1
        <if test="pc != null and pc != ''">
            AND PC = #{pc}
        </if>
        <if test="num != null and num != ''">
            AND NUM = #{num}
        </if>
        <if test="market != null and market != ''">
            AND MARKET = #{market}
        </if>
        <if test="variable != null and variable != ''">
            AND VARIABLE LIKE CONCAT('%', #{variable}, '%')
        </if>
        <if test="val != null and val != ''">
            AND VAL LIKE CONCAT('%', #{val}, '%')
        </if>
        <if test="vs != null and vs != ''">
            AND VS LIKE CONCAT('%', #{vs}, '%')
        </if>
        <if test="vs2 != null and vs2 != ''">
            AND VS2 LIKE CONCAT('%', #{vs2}, '%')
        </if>
        <if test="comments != null and comments != ''">
            AND COMMENTS LIKE CONCAT('%', #{comments}, '%')
        </if>
    </select>
    
    <!-- 根据市场查询变量 -->
    <select id="selectVariablesByMarket" resultType="string">
        SELECT VARIABLE FROM HDOC_USER_DEFINED_RULES WHERE MARKET = #{market}
    </select>
    
</mapper>
