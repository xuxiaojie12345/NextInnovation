<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.app.mapper.UD17Mapper">

    <select id="selectAllMarkets" resultType="java.util.LinkedHashMap">
        SELECT MARKET, DESCRIPTION FROM MARKET_MASTER ORDER BY MARKET
    </select>

    <select id="selectUsername" resultType="string">
        SELECT USERNAME FROM HDOC_USER_INFOR WHERE USERID = #{userid}
    </select>

    <select id="selectFunctionAuth" resultType="java.util.LinkedHashMap">
        SELECT `FUNCTION`, USERID FROM HDOC_FUNCTION_AUTH WHERE USERID = #{userid}
    </select>

    <select id="selectMarketAuth" resultType="java.util.LinkedHashMap">
        SELECT USERID, MARKET, TYPE, BU FROM HDOC_MARKET_AUTH
        WHERE USERID = #{userid}
    </select>

    <delete id="deleteAllFunctionAuth">
        DELETE FROM HDOC_FUNCTION_AUTH WHERE USERID = #{userid}
    </delete>

    <delete id="deleteAllMarketAuth">
        DELETE FROM HDOC_MARKET_AUTH WHERE USERID = #{userid}
    </delete>

    <insert id="insertFunctionAuth">
        INSERT INTO HDOC_FUNCTION_AUTH (`FUNCTION`, USERID,
            REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
            UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS)
        VALUES (#{function}, #{userid},
            NOW(), #{registerUser}, #{registerProcess},
            NOW(), #{registerUser}, #{registerProcess})
    </insert>

    <insert id="insertMarketAuth">
        INSERT INTO HDOC_MARKET_AUTH (USERID, MARKET, `TYPE`, BU,
            REGISTER_DATETIME, REGISTER_USER, REGISTER_PROCESS,
            UPDATE_DATETIME, UPDATE_USER, UPDATE_PROCESS)
        VALUES (#{userid}, #{market}, #{type}, #{bu},
            NOW(), #{registerUser}, #{registerProcess},
            NOW(), #{registerUser}, #{registerProcess})
    </insert>

</mapper>
