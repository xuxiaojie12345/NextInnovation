请根据以下要求生成[Homologation Variables]的详细设计文档：

1.基本信息
   文档编号:DES-[HomologationVariables]-008
   模块名称:Homologation Variables (认证参数管理画面)
   作成日:2026-05-28

2. 背景 (Background)
 请描述：
  本模块的核心功能和目标：管理车辆认证相关的用户定义参数规则。用户可以通过此画面查询、添加、更新、删除 RULES 情报。
  面向的用户群体：系统管理员、数据维护人员。
  用户体验方面的考虑
    1.提供直观的增删改查（CRUD）操作界面。
    2.清晰的按钮布局，区分搜索、清空、新增、更新、删除等操作。
    3.对关键操作（新增、更新、删除）提供即时的错误反馈，确保数据准确性。
    4.在新增时对 Variable 字段进行特殊处理（去除前缀并校验）。
  安全性或特殊要求：对输入数据进行严格校验，防止注入等安全风险。

3. 画面项目定义 (Screen Parameters)
请按照以下格式定义所有控件：

| No. | 項目名 (Item) | 種別 (Type) | 必須 (Req) | MaxLength | I/O | 許容文字 (Allowed Chars) | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注 |
|-----|--------------|-------------|-----------|-----------|-----|-------------------------|-----------------|-----------------|-------------------|------|
1	Product class	Pull-downList	Y	2	Input	半角英数字+記号	左	空	活性	Add/Update/Deleteの時、设为必填。数据源: PRODUCT_CLASS_MASTER.PC
2	Number	TextField	Y	10	Input	半角数字	左	空	活性	Add/Update/Deleteの時、设为必填
3	Market	Pull-downList	Y	3	Input	半角英数字+記号	左	空	活性	Add/Update/Deleteの時、设为必填。数据源: MARKET_MASTER.MARKET
4	Variable	TextField	-	20	Input	半角英数字+記号	左	空	活性	若输入内容格式为TEMPLATE-XXX，则去除前缀TEMPLATE-，并校验剩余内容是否已存在于HDOC_VARIABLES表中。数据源: HDOC_USER_DEFINED_RULES.VARIABLE
5	Value	TextField	-	200	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.VAL
6	Variant string.1	TextField	-	100	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.VS
7	Variant string.2	TextField	-	100	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.VS2
8	Comments	TextField	-	100	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.COMMENTS
9	Add	TextField	-	6	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.ADD_DATE
10	Delete	TextField	-	6	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.DELETE_DATE
11	Created by user	TextField	-	16	Input	半角英数字+記号	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.UPDATE_USER
12	Date	TextField	-	-	Input	日付	左	空	活性	数据源: HDOC_USER_DEFINED_RULES.UPDATE_DATETIME
13	Search	Button	-	-	Action	-	中	-	活性	执行查询操作
14	Clear	Button	-	-	Action	-	中	-	活性	清空所有输入字段
15	Add	Button	-	-	Action	-	中	-	活性	执行新增操作
16	Update	Button	-	-	Action	-	中	-	活性	执行更新操作
17	Delete	Button	-	-	Action	-	中	-	活性	执行删除操作

4. 业务逻辑与校验规则 (Business Logic & Checks)
请提供：
   4.1 处理流程
    按步骤描述每个主要功能的处理流程，格式如下：

    4.1.1 画面初始化 (Initial Load)
    功能说明：页面加载时，获取并填充下拉框选项。
    操作步骤：
      1. 从Menu画面迁移的场合：用户从 Menu 画面点击 "Update user defined variables (rules)" 链接，跳转到本画面。
      2. 从Homologation Variables的查询结果页迁移的场合：用户从认证参数查询结果页点击【back】按钮，返回到本画面。此时，系统需保存查询结果页返回的 "Product class", "Number", "Market" 的值，并将其他返回值回填道对应字段。
      3. 系统调用 UD08SelectProductclassmaster 方法，获取 PRODUCT_CLASS_MASTER 表中所有 PC 值，填充到 "Product class" 下拉框。
      4. 系统调用 UD08SelectMarketmaster 方法，获取 MARKET_MASTER 表中所有 MARKET 值，填充到 "Market" 下拉框。
      5. 下拉框加载完成，其他输入框和按钮处于待命状态。若为从结果页返回，则查询条件字段已预填充。

    4.1.2 新增操作 (Add Operation)
    功能说明：向数据库中添加一条新的用户定义规则记录。
      开始：用户点击 "Add" 按钮。
      前置处理：获取画面所有字段的当前值。
      空值校验等 (Frontend Check)：
        若 "Product class", "Number", "Market" 任一为空：
          设置 Message = "Product class, Number, and Market are required for Add operation."
          终止流程
      存在性检查 (Backend Check)：
          调用 UD08SelectUserDefinedRules API，传入 "Product class", "Number", "Market"。
          若 API 返回记录存在：
            设置 Message = "Primary key conflict, Please enter the correct content"
            终止流程。
      格式校验 (Frontend Check)：
        若 "Variable" 字段以 "TEMPLATE-" 开头，则去除前缀。
      API调用 (Backend Check)：
        调用 UD08SelectUserDefinedRules API，传入"Product class", "Number", "Market"，检查记录是否存在。
        调用 UD08SelectHdocvariables API，传入处理后的 "Variable" 值，校验其在 HDOC_VARIABLES 表中是否存在。
        调用 UD08Add API，传入所有字段值，执行新增。
      结果处理：
       成功：显示 "新增成功" 提示，刷新列表。
       失败：显示对应错误消息（"Primary key conflict..." 或 "Variant does not exist..."）。

    4.1.3 更新操作 (Update Operation)
    功能说明：更新数据库中已有的用户定义规则记录。
      开始：用户点击 "Update" 按钮。
      前置处理：获取画面所有字段的当前值。
      空值校验等 (Frontend Check)：
        若 "Product class", "Number", "Market" 任一为空：
          设置 Message = "Product class, Number, and Market are required for Update operation."
          终止流程
      存在性检查 (Backend Check)：
      从Homologation Variables的查询结果页返回的场合：
        若 "Product class", "Number", "Market" 的值和本地存储的值不一致：
          设置 Message = "Primary key conflict, Please enter the correct content"
          终止流程
        否则：
        调用 UD08SelectUserDefinedRules API，传入 "Product class", "Number", "Market"。
        若 API 返回记录不存在：
          设置 Message = "Data does not exist, Please enter the correct content"
          终止流程。
      格式校验 (Frontend Check)：
        若 "Variable" 字段以 "TEMPLATE-" 开头，则去除前缀。
      API调用 (Backend Check)：
        调用 UD08SelectUserDefinedRules API，传入"Product class", "Number", "Market"，检查记录是否存在。
        调用 UD08SelectHdocvariables API，传入处理后的 "Variable" 值，校验其在 HDOC_VARIABLES 表中是否存在。
        调用 UD08Update API，传入所有字段值，执行更新。
      结果处理：
       成功：显示 "更新成功" 提示。
       失败：显示对应错误消息（"Data does not exist..." 或 "Primary key conflict..." 或 "Variant does not exist..."）。

    4.1.4 删除操作 (Delete Operation)
    功能说明：从数据库中删除一条用户定义规则记录。
      开始：用户点击 "Delete" 按钮。
      前置处理：获取 "Product class", "Number", "Market" 字段的当前值。
      空值校验等 (Frontend Check)：
        若 "Product class", "Number", "Market" 任一为空：
          设置 Message = "Product class, Number, and Market are required for Delete operation."
          终止流程
      存在性检查 (Backend Check)：
        调用 UD08SelectUserDefinedRules API，传入 "Product class", "Number", "Market"。
        若 API 返回记录不存在：
          设置 Message = "Data does not exist, Please enter the correct content"
          终止流程。
      API调用 (Backend Check)：
        调用 UD08SelectUserDefinedRules API，传入"Product class", "Number", "Market"，检查记录是否存在。
        调用 UD08Delete API，传入 "Product class", "Number", "Market" 值，执行删除。
      结果处理：
       成功：显示 "删除成功" 提示。
       失败：显示对应错误消息（"Data does not exist..."）。

    4.1.5 查询操作 (Search Operation)
    功能说明：根据输入的查询条件执行数据检索，并跳转至查询结果页面。
    操作步骤：
      1. 用户在画面任意字段输入查询条件。
      2. 用户点击 "Search" 按钮。
      3. 系统将输入的查询条件作为参数，携带至认证参数查询结果页。
      4. 系统执行业务逻辑校验（检查主键重复，校验 Variable 是否存在）。
      5. 页面跳转至认证参数查询结果页，并显示符合查询条件的数据。

    4.1.6 清空操作 (Clear Operation)
    功能说明：清空所有输入字段的内容。
    操作步骤：
      1. 用户点击 "Clear" 按钮。
      2. 系统将所有输入字段（TextField, Pull-downList）的值清空。
      3. 所有输入字段变为空。

   4.2 结果处理：
       成功：根据操作类型（Add/Update/Delete/Search）显示成功提示或更新画面数据。
       失败：在画面显眼位置（如顶部或底部）显示具体的错误消息。

   4.3 校验详细规格表
   | No. | 检查时机 | 检查对象 | 检查条件 | 错误消息 (Message Content) | 动作 |
   |-----|---------|---------|---------|--------------------------|------|
   1	Add 按钮点击	主键组合 (Product class, Number, Market)	数据库中已存在相同组合的记录	Primary key conflict, Please enter the correct content	显示错误消息，阻止提交
   2	Add 按钮点击	Variable 字段	Variable 去除前缀后，在 HDOC_VARIABLES 表中不存在	Variant does not exist, Please enter the correct content	显示错误消息，阻止提交
   3	Update 按钮点击	待更新记录	根据 (Product class, Number, Market) 无法查询到现有记录	Data does not exist, Please enter the correct content	显示错误消息，阻止提交
   4	从查询结果页返回后，修改主键	主键组合 (Product class, Number, Market)	用户修改了从结果页带回的主键值，尝试进行 Update 操作时	Primary key conflict, Please enter the correct content	显示错误消息，阻止提交
   5	Update 按钮点击	Variable 字段	Variable 去除前缀后，在 HDOC_VARIABLES 表中不存在	Variant does not exist, Please enter the correct content	显示错误消息，阻止提交
   6	Delete 按钮点击	待删除记录	根据 (Product class, Number, Market) 无法查询到现有记录	Data does not exist, Please enter the correct content	显示错误消息，阻止提交

5. 接口定义 (API Specification)
请为每个API提供(TODO：API的具体内容参照API提示词)：

   [UD08SelectProductclassmaster]
     功能：获取产品类别主数据列表。
     Method：GET
     Endpoint：/api/ud08/selectproductclassmaster

     Request 无参数

     Response Success (200)
     {
        "code": 200,
        "message": "Success",
        "data": [
          {
            "pc": "PC01"
          },
          {
            "pc": "PC02"
          }
        ]
     }

     Response Error (400/500)
     {
        "code": 500,
        "message": "System Error",
        "data": null
     }


   [UD08SelectMarketmaster]
     功能：获取市场主数据列表。
     Method：GET
     Endpoint：/api/ud08/selectmarketmaster

     Request 无参数
     
     Response Success (200)
     {
       "code": 200,
       "message": "Success",
       "data": [
         {
           "market": "DE"
         },
         {
           "market": "US"
         }
       ]
     }

     Response Error (400/500)
     {
        "code": 500,
        "message": "System Error",
        "data": null
     }

   [UD08SelectUserDefinedRules]
      功能：根据主键组合查询 HDOC_USER_DEFINED_RULES 表，用于存在性检查。
      Method：GET
      Endpoint：/api/ud08/selectuserdefinedrules
      Request [json]
      {
        "pc": "string",
        "number": "string",
        "market": "string"
      }

      Response Success (200)
      {
        "code": 200,
        "message": "Success",
        "data": {
          "count": integer
        }
      }

     Response Error (400/500)
     {
        "code": 500,
        "message": "System Error",
        "data": null
     }

   [UD08SelectHdocvariables]
    功能：根据 Variable 值查询 HDOC_VARIABLES 表，用于校验是否存在。
    Method：GET
    Endpoint：/api/ud08/selecthdocvariables

    Request [json]
    {
      "variable": "string"
    }

    Response Success (200)
    {
        "code": 200,
        "message": "Success",
        "data": {
          "count": integer
        }
      }

     Response Error (400/500)
     {
        "code": 500,
        "message": "System Error",
        "data": null
     }

   [UD08Add]
    功能：新增用户定义规则记录。
    Method：POST
    Endpoint：/api/ud08/add
    Request [json]
    {
      "pc": "string",
      "number": "string",
      "market": "string",
      "variable": "string",
      "val": "string",
      "vs": "string",
      "vs2": "string",
      "comments": "string",
      "addDate": "string",
      "deleteDate": "string",
      "updateUser": "string",
      "updateDatetime": "datetime"
    }

     Response Error (200)
     {
        "code": 200,
        "message": "Added successfully"
     }

     Response Error (400/500)
     {
        "code": 400,
        "message": "Primary key conflict, Please enter the correct content",
        "data": null
     }

   [UD08Update]
    功能：更新用户定义规则记录。
    Method：POST
    Endpoint：/api/ud08/update
    Request [json]
    {
      "pc": "string",
      "number": "string",
      "market": "string",
      "variable": "string",
      "val": "string",
      "vs": "string",
      "vs2": "string",
      "comments": "string",
      "addDate": "string",
      "deleteDate": "string",
      "updateUser": "string",
      "updateDatetime": "datetime"
    }

     Response Error (200)
     {
        "code": 200,
        "message": "Updated successfully"
     }

     Response Error (400/500)
     {
        "code": 400,
        "message": "Data does not exist, Please enter the correct content",
        "data": null
     }
     
    [UD08Delete]
    功能：删除用户定义规则记录。
    Method：POST
    Endpoint：/api/ud08/delete

    Request [json]
    {
      "pc": "string",
      "number": "string",
      "market": "string"
    }

     Response Error (200)
     {
        "code": 200,
        "message": "Deleted successfully"
     }

     Response Error (400/500)
     {
        "code": 400,
        "message": "Data does not exist, Please enter the correct content",
        "data": null
     }

6. 异常处理 (Exception Handling)（TODO：共同化可能）
| 异常场景 | 处理方式 | Message显示内容 |
|---------|---------|----------------|
网络请求失败	前端捕获网络错误	"Network error. Please check your connection."
服务器内部错误 (500)	捕获异常，记录日志	"System error. Please contact support."
数据库操作异常 (非业务逻辑)	捕获异常	"Database operation failed. Please try again later."

7.语言要求
  文档使用中文编写
  所有错误消息和成功消息使用中文
  技术术语可保留英文
  保持与参考文档（内部設計模板.md）相同的格式结构

8.输出要求
  生成完整的Markdown格式文档
  文件名格式：[模块功能名]_詳細設計.md
  保存到：当前文件夹下的work目录
