# Login画面详细设计说明书

| 文档编号     | UD01                    | 版本号     | v1.0       |
| :----------- | :---------------------- | :--------- | :--------- |
| **模块名称** | EDB登录认证 (EDB Login) | **作成日** | 2026-05-26 |
| **作成者**   | AI Assistant            | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块是 Engineering Database (EDB) 系统的入口，负责验证用户身份。用户通过输入 Outlook ID (UserID) 和密码 (Password)，经由前端校验后调用后端 ISAM 接口 (`AuthenticationApi`) 进行认证。

- **目标**：确保只有授权用户能进入系统，并提供清晰的错误反馈及备用登录指引。
- **安全性**：密码字段需掩码显示，所有敏感数据传输需通过 HTTPS 加密。
- **用户体验**：
  - 界面采用左右分栏设计：左侧展示品牌与支持信息，右侧聚焦登录操作。
  - 采用 Label 形式在页面内（Login 按钮上方）显示错误信息，避免频繁弹窗打断操作流。
  - 支持键盘快捷键（Enter 键触发登录）。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| 項目名 (Item) | 種別 (Type) | 必須 (Req) | MaxLength |  I/O   | 許容文字 (Allowed Chars)          | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注                                                   |
| :------------ | :---------- | :--------: | :-------: | :----: | :-------------------------------- | :--------------: | :--------------: | :----------------: | :----------------------------------------------------- |
| **UserID**    | TextField   |     Y      |    10     | Input  | 半角英数字 (`a-z, A-Z, 0-9`)      |    左 (Left)     |    空 (Empty)    |   活性 (Enabled)   | 示例：A451340；自动去除首尾空格                        |
| **Password**  | TextField   |     Y      |    32     | Input  | 半角英数字 + 記号 (Special Chars) |    左 (Left)     |    空 (Empty)    |   活性 (Enabled)   | 输入时显示为掩码 (`•`)                                 |
| **Message**   | Label       |     -      |    256    | Output | 任意可见字符                      |    左 (Left)     |    空 (Empty)    |      动态显示      | 仅在出错时显示红色文字，位于 Password 下方、Login 上方 |
| **Login**     | Button      |     -      |     -     | Action | -                                 |   中 (Center)    |        -         |   活性 (Enabled)   | 点击触发校验与 API 调用                                |

> **注**：
>
> - **半角英数字**：正则表达式 `[a-zA-Z0-9]`
> - **記号**：指 ASCII 范围内的可见符号（如 `!@#$%` 等），具体范围以后端安全策略为准。
> - **界面布局补充**：
>   - 背景：
>     1. `背景为一张图片（暂不提供）
>   - 左侧区域文字白色（不要背景颜色，直接在背景图片上显示）：
>     1. `EDB Engineering Database` (标题，EDB加粗，靠左)
>     2. `Use Outlook id and password` (居中)
>     3. `Support, authorization request or improvement suggestions, send mail to: Support.TPI` (靠左)
>   - - 右侧区域：1.登录表单不要背景颜色，直接在背景图片上显示，不要标题提示，直接入力框和按钮显示；底部备用链接提示（以下文字全部都是红色字体，不要背景颜色，直接在背景图片上显示）：
>       `If you get error message: "Your account is locked. Please contact your system administrator."`
>       `Please try this alternative login link before contacting support: Login`
>       `We are working to find root cause of problem.`

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

所有校验逻辑均在用户点击 **[Login]** 按钮或按下 Enter 键时执行。

### 3.1 处理流程

1.  **开始**：用户点击 [Login] 按钮或按 Enter 键。
2.  **前置处理**：获取 UserID 和 Password，执行 `trim()` 去除首尾空白。
3.  **空值校验 (Frontend Check)**：
    - 若 `UserID` 为空 **或** `Password` 为空：
      - 设置 `Message` = `Username and password are required.`
      - **终止**流程，不调用 API。
4.  **API 调用 (Backend Check)**：
    - 若校验通过，调用 `POST /api/AuthenticationApi/login`。
5.  **结果处理**：
    - **认证成功 (200)**：清空 Message，保存 Token，跳转至 `/dashboard`。
    - **认证失败 (401/403)**：
      - 设置 `Message` = `We didn't recognize the username or password you entered. Please try again.`
      - 建议清空 Password 字段以便重新输入。

### 3.2 校验详细规格表

| No. |   检查时机   |     检查对象      |                 检查条件                 |                          错误消息 (Message Content)                           |                    动作                     |
| :-: | :----------: | :---------------: | :--------------------------------------: | :---------------------------------------------------------------------------: | :-----------------------------------------: |
|  1  | Click Login  | UserID / Password |  `Value == null` OR `Trim(Value) == ""`  |                     `Username and password are required.`                     |    显示 Message，焦点停留在第一个空字段     |
|  2  | API Response |    Auth Result    | `Status != Success` OR `Code == 401/403` | `We didn't recognize the username or password you entered. Please try again.` | 显示 Message (红色)，建议清空 Password 字段 |

## 4. 接口定义 (API Specification)

### 4.1 AuthenticationApi

- **功能**：验证用户凭证并返回认证结果。
- **Method**: `POST`
- **Endpoint**: `/api/AuthenticationApi/login`

#### Request Body

```json
{
  "userId": "string (Max 10, Alphanumeric)",
  "password": "string (Max 32, Alphanumeric + Symbols)"
}
```

#### Response Success (200 OK)

```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "A451340",
    "name": "张三",
    "role": "Engineer"
  }
}
```

#### Response Error (401 Unauthorized / 403 Forbidden)

```json
{
  "error": "Unauthorized",
  "message": "Invalid credentials"
}
```

## 5. 异常处理 (Exception Handling)

| 异常场景                 | 处理方式           | Message 显示内容                                            |
| :----------------------- | :----------------- | :---------------------------------------------------------- |
| **网络断开/超时**        | 捕获 Network Error | `Network error. Please check your connection.`              |
| **账户被锁定 (403)**     | 显示特定提示       | `您的账户已被锁定，请联系系统管理员` (配合底部备用链接显示) |
| **服务器内部错误 (500)** | 捕获 Server Error  | `System error. Please contact administrator.`               |
| **输入非法字符**         | 前端实时拦截       | 不允许输入非定义范围内的字符                                |

## 6. 实现注意事项 (Implementation Notes)

1.  **状态管理**：
    - 使用 React State 管理 `userID`, `password`, `message`, `isLoading`。
    - `Message` Label 默认隐藏（内容为空时不占位或高度为0），出错时显示红色文字。
2.  **安全性**：
    - 严禁在前端日志中打印明文密码。
    - API 请求必须通过 HTTPS 发送。
3.  **UI 细节**：
    - [Login]按钮在 `isLoading` 期间应设为 `disabled`，防止重复提交。
    - 左侧品牌区域背景色需与右侧登录区域形成对比，确保视觉清晰。
