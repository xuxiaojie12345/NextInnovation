请严格按照以下要求，生成所有【API】的逻辑代码

1.Entity
根据这个文档生成一下文件 F:\★王建徳\tongyilingma_test\NextInnovation\api-ud\src\react_ud_sql.txt    
    1.1.生成相关的entity实体类，以文件中每个表名进行创建，命名规则使用驼峰名规则，如表【user】，实体类名为User
根据这个文档只生成Entity

2.整体架构
    2.1 技术栈
    核心框架: Spring Boot 2.7.6
    持久层: MyBatis 2.2.2 + Druid 1.2.5 连接池
    数据库: MySQL
    API 文档: Swagger 3.0.0 (Springfox)
    日志框架: Log4j2 (替代默认日志系统)
    构建工具: Maven
    编程语言: Java 1.8
    辅助库: Lombok, PageHelper, commons-lang

    2.2 项目结构
    ├── src
    │   └── main
    │       ├── java
    │       │   └── com
    │       │       └── web
    │       │           └── app
    │       │               ├── config           配置类
    │       │               ├── controller       控制器层
    │       │               ├── domain           领域模型目录
    │       │               ├── dto              数据传输对象
    │       │               ├── entity           实体类
    │       │               ├── mapper           数据访问层
    │       │               ├── service          业务逻辑层
    │       │               │   └── impl         业务逻辑实现
    │       │               └── Application.java  主启动类
    │       └── resources
    │           ├── mapper                     MyBatis 映射文件
    │           ├── sql                        SQL 脚本
    │           ├── application-dev.yml        开发环境配置
    │           ├── application-prod.yml       生产环境配置
    │           ├── application-test.yml       测试环境配置
    │           ├── application.yml            主配置文件
    │           ├── log4j2-dev.xml             开发环境日志配置
    │           ├── log4j2-prod.xml            生产环境日志配置
    │           └── log4j2-test.xml            测试环境日志配置
    ├── README.md
    ├── mvnw.cmd
    └── pom.xml

2.3 模块组成
    控制器: [XXXXXController] (src/main/java/com/web/app/controller/XXXXXController.java) - 处理认证请求
    服务接口: [XXXXXService](src/main/java/com/web/app/service/XXXXXService.java) - 定义认证服务方法
    服务实现: [XXXXXServiceImpl](src/main/java/com/web/app/service/impl/XXXXXServiceImpl.java) - 实现认证逻辑
    数据访问: [UserMapper](src/main/java/com/web/app/mapper/UserMapper.xml)+ [UserMapper.xml](src/main/resources/mapper/UserMapper.xml) - 用户数据访问
    数据传输对象: [LoginRequest]()/[LoginResponse]() - 认证请求/响应对象
    实体类: [User](src/main/java/com/web/app/entity/User.java) - 用户实体

3. API功能概述
    3.1.[AuthenticationApi]:用户信息数据取得
    3.2.[UD03SelectHdocdocumentlistApi]:文件类型的查询
    3.3.[UD04SelectGeneratedocumentApi]:Generate document画面信息查询
    3.4.[UD05ModifyDocumentApi]:Modify Document画面信息查询,修改
    3.5.[UD06SaveModificationsApi]:Save Modifications画面信息查询
    3.6.[UD07VehicleSpecificationApi]:VDA - Vehicle Specification画面信息查询
    3.7.[UD08HomologationVariablesApi]:Homologation Variables画面信息的查询，新增，更新，删除
    3.8.[UD09DeleteHdocuserdefinedrulesApi]:Homologation Variables画面信息的查询，删除
    3.9.[UD10HdocvariablesApi]:Existing HDoc Varriables画面信息的新增，更新，删除
    3.10.[UD11HdocvariablesApi]:Homologation Variables画面信息检索
    3.11.[UD12UploadDeletetemplatApi]:Upload&Delete template画面信息检索，更新，删除
    3.12.[UD14SearchresultistApi]: List available templates画面信息检索
    3.13.[UD15SelecthdocsenddatavinplateApi]: Vin Plate画面的检索，更新
    3.14.[UD16ADChangeApi]: AD Change画面的检索，追加，删除
    3.15.[UD16ADChangeApi]: HDoc User Administration画面的检索，更新，删除
    3.16.[UD18HDocUserDocAdministrationApi]: HDoc User Doc Administration画面的更新，登录
    3.17.[UD19SearchResultListApi]: Search HDoc User画面的检索
    3.18.[UD20MarketDocumentSettingsApi]: HDoc - Market Document Setting検索結果画面

    ............
        
4. 处理流程
   【AuthenticationApi】
    4.1 客户端通过GET请求访问接口 /api/AuthenticationApi/login，将用户名、密码等请求参数，以JSON格式发送至后端服务。

    4.2 后端【LoginController】接收前端请求，通过【LoginRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【LoginService】接口中的 login() 方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【LoginServiceImpl】业务实现层执行核心登录逻辑：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 对密码格式、账号状态进行前置校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【***Mapper】数据访问层，根据用户名查询数据库中的用户信息，获取用户实体对象。

    4.6 登录核心验证逻辑：
        • 判断用户是否存在，不存在则返回“账号不存在”
        • 比对前端传递密码与数据库加密密码是否一致
        • 密码错误则返回“密码不正确”

    4.7 验证全部通过后：
        • 构建登录成功响应体

    4.8 封装【LoginResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "登录成功",
            "data": {
                "userId": "xxxx",
                ······
            }
        }
----------------------------------------------------------------------------------------------------------------

   【UD03SelectHdocdocumentlistApi】
    4.1 客户端通过POST请求访问接口 /api/UD03SelectHdocdocumentlistApi/types，调用后端服务。

    4.2 后端【SelectHdocdocumentlistController】接收前端请求。

    4.3 控制器层调用【SelectHdocdocumentlistService】接口中的 SelectHdocdocumentlist() 方法。

    4.4 【SelectHdocdocumentlistServiceImpl】业务实现层执行核心查询逻辑：
        • 调用Mapper层，取得数据库数据。

    4.5 业务层通过【***Mapper】数据访问层，查询数据库中的全部文件类型信息。

    4.6 查询核心验证逻辑：
        • 判断文件类型是否存在，不存在则返回“Chassis no is not exists”
    4.7 检索完成后：
        • 建成文档类型列表,返回数据

    4.8 封装【SelectHdocdocumentlistResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "查询成功",
            "data": [
                          {
                "doctyp": "xxxx"
               }
                        ]
        }
----------------------------------------------------------------------------------------------------------------
   【UD04SelectGeneratedocumentApi】
    4.1 客户端通过POST请求访问接口 /api/UD04SelectGeneratedocumentApi/SelectGeneratedocument，将请求参数，以JSON格式发送至后端服务。

    4.2 后端【SelectGeneratedocumentController】接收前端请求，通过【SelectGeneratedocumentRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【SelectGeneratedocumentService】接口中的 SelectGeneratedocument() 方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【SelectGeneratedocumentServiceImpl】业务实现层执行核心登录逻辑：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【***Mapper】数据访问层，根据传入参数查询数据库中的信息，获取实体对象。

    4.6 查询核心验证逻辑：
        • 查询需要的数据
    4.7 验证查询全部通过后：
        • 生成返回值

    4.8 封装【SelectGeneratedocumentResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "查询成功",
            "data": {
                   "chassisNo": "JPCT_013945",
                   "ordernumber": "15101319",
                   "buildWeek": "2016173",
                   "specWeek": "201617",
                   "market": "IDO",
                   "masterMarket": "-EU",
                   "sNotes": ["S161011", "S1610174"],
                   "sNoteMessage": "The S-Notes above can affect homologation documents.",
                   "loadIndex": "FTLI-150",
                   "speedIndex": "FTSI-K",
                   "adChangeEnabled": true,
                   "templateName": "_eu/VIN_PLATE_UD_TRUCKS_TSA_INDO_PHIL.rtf",
                   "replacedParams": ["AXLE_CONF", "WB_MM"],
                   "generatedFileUrl": "/download/vinplate/JPCT_013945.rtf",
                   "date": "2022-12-06 07:30:31",
                   "hdocVersion": "4.2.1"
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD05ModifyDocumentApi】
    4.1 Select的场合：客户端通过POST请求访问接口 /api/UD05ModifyDocumentApi/UD05SelectVariableModification，将SERIE、CHNO请求参数，以JSON格式发送至后端服务。
        Update的场合：客户端通过POST请求访问接口 /api/UD05ModifyDocumentApi/UD05UpdateHdocAdcaModification，将SERIE、CHNO、DESCRIPTION请求参数，以JSON格式发送至后端服务。

    4.2 后端【UD05ModifyDocumentController】接收前端请求，通过【UD05ModifyDocumentRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD05ModifyDocumentService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD05ModifyDocumentServiceImpl】业务实现层执行核心登录逻辑：
         Select的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 校验不通过则直接返回参数异常响应
         Update的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【***Mapper】数据访问层，根据传入参数查询数据库中的信息。

    4.6 查询的核心验证逻辑：
         Select的场合：
        • 判断查询的数据是否存在，不存在则返回“数据不存在”
         Update的场合：
        • 查询更新的数据数据是否存在，不存在则返回“更新失败”

    4.7 验证全部通过后：
         Select的场合：
        • 生成返回数据集合
         Update的场合：
        • 返回更新成功

    4.8 封装【UD05ModifyDocumentResponse】响应对象，统一返回标准格式：
         Select的场合：
        {
            "code": 200,
            "msg": "查询成功",
            "data": {
                "***": "xxxx"
                ······
            }
        }
         Update的场合：
        {
            "code": 200,
            "msg": "更新成功",
            "data": {
                "msg": "更新成功"
                 ······
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD06SaveModificationsApi】
    4.1 客户端通过POST请求访问接口 /api/UD06SaveModificationsApi/UD06SelectHdocAdcaModification，将Chassis serie、Chassis number请求参数，以JSON格式发送至后端服务。

    4.2 后端【UD06SelectHdocAdcaModificationController】接收前端请求，通过【UD06SelectHdocAdcaModificationRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD06SelectHdocAdcaModificationService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD06SelectHdocAdcaModificationServiceImpl】业务实现层执行核心登录逻辑：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【***Mapper】数据访问层，根据用户名查询数据库中的用户信息，获取实体对象。

    4.6 查询核心验证逻辑：
        • 判断数据是否存在，不存在则返回“数据不存在”

    4.7 验证全部通过后：
        • 构建查询成功响应体

    4.8 封装【UD06SelectHdocAdcaModificationResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "查询成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD07VehicleSpecificationApi】
    4.1 客户端通过POST请求访问接口 /api/UD07VehicleSpecificationApi/Select，将chassisNo请求参数，发送至后端服务。

    4.2 后端【UD07VehicleSpecificationController】接收前端请求，校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD07VehicleSpecificationService】接口中的方法，将校验后的请求参数传递至业务逻辑层处理。

    4.4 【UD07VehicleSpecificationServiceImpl】业务实现层执行核心登录逻辑：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【***Mapper】数据访问层，根据用户名查询数据库中的信息。

    4.6 登录核心验证逻辑：
       • 根据 chassisNo 查询 HDOC_REC_DATA_VDA_VARIANTS 表获取 FAMILY_ID, VARIANT_ID
       • 根据上一步获取的 FAMILY_ID, VARIANT_ID 查询 HDOC_REC_DATA_KOLA_VARIANT 表获取 SYMBOL, FUNCTION_GROUP, DESCRIPTION
       • 对 HDOC_REC_DATA_KOLA_VARIANT 返回的 SYMBOL 进行格式化处理：截取前8位，不足8位时右侧补充半角空格
       • 组装所有查询到的数据（Model, Build Week, VIN, Engine No, Country Of Operation, S-Note NO, 格式化后的 SYMBOL_STR, DESCRIPTION）

    4.7 验证全部通过后：
        • 构建查询成功响应体，

    4.8 封装【UD07VehicleSpecificationResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "查询成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                ······
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD08HomologationVariablesApi】
    4.1 客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08SelectProductclassmaster，取得数据库中PRODUCT_CLASS_MASTER表的数据。
                客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08SelectMarketmaster，取得数据库中MARKET_MASTER表的数据。
                客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08SelectHdocvariables，取得数据库中HDOC_VARIABLES表的数据。
                客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08Add，登录HDOC_USER_DEFINED_RULE表的数据。
                客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08Update，根据更新条件，更新HDOC_USER_DEFINED_RULES表中的数据。
                客户端通过GET请求访问接口 /api/UD08HomologationVariablesApi/UD08Delete，根据删除条件，删除HDOC_USER_DEFINED_RULES表中的数据。

    4.2 后端【UD08HomologationVariableController】接收前端请求（根据不同的接口，不同的操作，生成相应的方法）。

    4.3 控制器层调用【UD08HomologationVariableService】接口中的方法（根据不同的接口，不同的操作，生成相应的方法）。

    4.4 【UD08HomologationVariableServiceImpl】业务实现层执行核心逻辑：
       初期表示的场合：
        • 查询 PRODUCT_CLASS_MASTER 表获取 PC 列表  
        • 查询 MARKET_MASTER 表获取 MARKET 列表
    add、update的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 执行存在性检查（调用 selectUserDefinedRules）
        • 执行 Variable 校验（调用 selectHdocVariables）
        • 校验不通过则直接返回参数异常响应
    delete的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 执行存在性检查（调用 selectUserDefinedRules），若记录不存在则返回错误
        • 校验不通过则直接返回参数异常响应。

    4.5 业务层通过【***Mapper】数据访问层
      初期表示的场合
        业务层通过【ProductclassmasterMapper】数据访问层，执行 SQL 查询，从 PRODUCT_CLASS_MASTER 表中检索数据。
        业务层通过【MarketmasterMapper】数据访问层，执行 SQL 检索，从 MARKET_MASTER 表中检索数据。
      add的场合
        业务层通过【UserdefinedrulesMapper】数据访问层，根据 pc, number, market 查询数据库中的 HDOC_USER_DEFINED_RULES 表。
        业务层通过【HdocvariablesMapper】数据访问层，根据 variable 查询数据库中的 HDOC_VARIABLES 表。
        业务层通过【UserdefinedrulesMapper】数据访问层，向 HDOC_USER_DEFINED_RULES 表插入一条新记录。
      update的场合
        业务层通过【UserdefinedrulesMapper】数据访问层，根据 pc, number, market 查询数据库中的 HDOC_USER_DEFINED_RULES 表。
        业务层通过【HdocvariablesMapper】数据访问层，根据 variable 查询数据库中的 HDOC_VARIABLES 表。
        业务层通过【UserdefinedrulesMapper】数据访问层，根据主键 (pc, number, market) 更新 HDOC_USER_DEFINED_RULES 表中的记录。
      delete的场合
        业务层通过【UserdefinedrulesMapper】数据访问层，根据 pc, number, market 查询数据库中的 HDOC_USER_DEFINED_RULES 表。
        业务层通过【UserdefinedrulesMapper】数据访问层，根据主键 (pc, number, market) 删除 HDOC_USER_DEFINED_RULES 表中的记录。

    4.6 核心验证逻辑：
        • 查询的场合：查询数据库中的信息，获取实体对象。
        • 新增的场合：往数据库中登录新的数据。
        • 更新的场合：根据更新条件，对符合的数据进行更新操作。
        • 删除的场合：根据删除条件，删除符合条件的数据。

    4.7 封装【UD08SelectProductclassmasterResponse】响应对象，统一返回标准格式：
        • 查询的场合
        {
            "code": 200,
            "msg": "查询成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }

        • 新增的场合：
        {
            "code": 200,
            "msg": "登录成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }

        • 更新的场合：
        {
            "code": 200,
            "msg": "更新成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }

        • 删除的场合：
        {
            "code": 200,
            "msg": "删除成功",
            "data": {
                "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }
----------------------------------------------------------------------------------------------------------------
    【UD09DeleteHdocuserdefinedrulesApi】
    4.1 客户端通过POST请求访问接口 /api/UD09DeleteHdocuserdefinedrulesApi/UD09Seach，将productClass、number、market、variable、value、string1、string2、comments等查询参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD09DeleteHdocuserdefinedrulesApi/UD09DeleteSelected，将选中的记录信息（selectedRecords）以JSON格式发送至后端服务。

    4.2 后端【UD09DeleteHdocuserdefinedrulesController】接收前端请求，通过【UD09DeleteHdocuserdefinedrulesRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD09DeleteHdocuserdefinedrulesService】接口中的UD09Seach()、UD09DeleteSelected() 方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD09DeleteHdocuserdefinedrulesServiceImpl】业务实现层执行核心逻辑：
      select的场合
       • 对请求参数进行非空、长度、格式等合法性校验 • 准备查询条件（根据传入参数构建动态SQL） • 校验不通过则直接返回参数异常响应
      delete的场合
       • 对请求参数进行非空、完整性等合法性校验 • 检查 selectedRecords 是否至少包含一条记录 • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【UserdefinedrulesMapper】数据访问层执行以下操作：
      select的场合
        根据查询条件查询数据库中的认证参数信息，获取用户定义规则实体对象。
      delete的场合
        根据选中记录的关键信息（PC、NUM、MARKET）删除数据库中的认证参数信息。

    4.6 封装【UD09DeleteHdocuserdefinedrulesResponse】响应对象，统一返回标准格式：
    select的场合
      {
        "code": 200,
        "msg": "Success",
        "data": {
        "records": [
            {
                "pc": "PC01",
                "num": "NUM001",
                "market": "DE",
                "variable": "VARIABLE_NAME",
                "val": "VALUE",
                "vs": "VARIANT1",
                "vs2": "VARIANT2",
                "comments": "COMMENT TEXT",
                "add_date": "2024-01-01",
                "delete_date": null,
                "register_user": "USER001",
                "register_datetime": "2024-01-01 10:00:00"
            }
         ],
         "count": 1
         }
       }
    delete的场合
       {
        "code": 200,
        "msg": "记录删除成功",
        "data": {
        }
       }
----------------------------------------------------------------------------------------------------------------

   【UD10HdocvariablesApi】
    4.1 • 登录的场合：客户端通过POST请求访问接口 /api/UD10HdocvariablesApi/UD10Add，将请求参数，以JSON格式发送至后端服务。
        • 更新的场合：客户端通过POST请求访问接口 /api/UD10HdocvariablesApi/UD10Update，将请求参数，以JSON格式发送至后端服务。
        • 删除的场合：客户端通过POST请求访问接口 /api/UD10HdocvariablesApi/UD10Delete，将请求参数，以JSON格式发送至后端服务。

    4.2 • 登录的场合：后端【UD10HdocvariablesController】接收前端请求，通过【UD10AddRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。
        • 更新的场合：后端【UD10HdocvariablesController】接收前端请求，通过【UD10UpdateRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。
        • 删除的场合：后端【UD10HdocvariablesController】接收前端请求，通过【UD10DeleteRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 • 登录的场合：控制器层调用【UD10HdocvariablesService】接口中UD10Add()的方法，将校验后的请求对象传递至业务逻辑层处理。
        • 更新的场合：控制器层调用【UD10HdocvariablesService】接口中UD10Update()的方法，将校验后的请求对象传递至业务逻辑层处理。
        • 删除的场合：控制器层调用【UD10HdocvariablesService】接口中UD10Delete()的方法，将校验后的请求对象传递至业务逻辑层处理。


    4.4 【UD10HdocvariablesServiceImpl】业务实现层执行核心登录逻辑：
        • 登陆的场合：判断要登陆的数据是否存在，存在则返回“Variant already exists. Please enter the correct content”
        • 更新的场合：判断要更新的数据是否存在，不存在则返回“Variant does not  exists. Please enter the correct content”
        • 删除的场合：判断要删除的数据是否存在，不存在则返回“Variant does not  exists. Please enter the correct content”

    4.5 业务层通过【***Mapper】数据访问层，获取用户实体对象。

    4.6 登录核心验证逻辑：
        • 登陆的场合：将数据登录到HDOC_VARIABLES表中。
        • 更新的场合：按照更新条件，将符合的数据更新到HDOC_VARIABLES表中。
        • 删除的场合：将符合数据在HDOC_VARIABLES表中删除。

    4.7 验证全部通过后：
        执行数据库更新操作

    4.8 • 登录的场合：封装【UD10AddResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "登录成功",
            "data": {
                 "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }

               • 更新的场合：封装【UD10UpdateResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "更新成功",
            "data": {
                 "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }

               • 删除的场合：封装【UD10DeleteResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "更新成功",
            "data": {
                 "***": "xxxx",
                "***": "xxxx",
                "***": "xxxx",
                                 ······
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD11HdocvariablesApi】
    4.1 客户端通过POST请求访问接口 /api/UD11HdocvariablesApi/UD11Search，将上一页面传递的查询条件参数，以JSON格式发送至后端服务。

    4.2 后端【UD11HdocvariablesController】接收前端请求，通过【UD11HdocvariablesRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【Ud11HdocvariablesService】接口中的 searchVariables() 方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 业务实现层执行核心搜索逻辑：
       • 对请求参数进行非空、长度、格式等合法性校验
       • 验证查询条件的有效性
       • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【HdocVariableMapper】数据访问层，根据查询条件查询数据库中的HDoc变量记录。

    4.6 搜索核心验证逻辑：
       • 检查查询条件是否有效

    4.7 验证全部通过后：
       • 执行数据库查询操作
       • 统计查询结果数量
       • 构建搜索成功响应体，包含查询结果和统计信息

    4.8 封装【UD11HdocvariablesResponse】响应对象，统一返回
     {
     "code": 200,
     "msg": "查询成功",
     "data": {
        "variables": [
          {
            "variable": "string",
            "type": "string",
            "description": "string",
            "createdByUser": "string",
            "date": "string"
          }
        ],
        "count": 0
     }
    }

----------------------------------------------------------------------------------------------------------------
   【UD12UploadDeletetemplatApi】
    4.1 客户端通过POST请求访问接口 /api/UD12UploadDeletetemplatApi/UD12SelectMarket，将上一页面传递的查询条件参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD12UploadDeletetemplatApi/UD12UploadFlie，将文件和市场信息以multipart/form-data格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD12UploadDeletetemplatApi/UD12DeleteFlie，将市场和模板文件名以JSON格式发送至后端服务。

    4.2 后端【UD12UploadDeletetemplatController】接收前端请求，通过【UD12UploadDeletetemplatRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD12UploadDeletetemplatService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【Ud12UploadDeletetemplatServiceImpl】业务实现层执行核心文件删除逻辑：：
      初期表示的场合：
       • 验证请求参数的有效性
       • 校验不通过则直接返回参数异常响应
      upload的场合：
       • 对请求参数进行非空、长度、格式等合法性校验
       • 验证Template File是否为空，为空则返回"NO FILE UPLOADED"
       • 验证Market是否为空，为空则返回"请选择目标市场"
       • 校验不通过则直接返回参数异常响应
      delete的场合：
       • 对请求参数进行非空、长度、格式等合法性校验
       • 验证Market和Template是否为空
       • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【Mapper】数据访问层执行以下操作：
     初期表示的场合：
       业务层通过【MarketMasterMapper】数据访问层，查询数据库中的市场主数据记录。
     upload的场合：
       业务层将文件保存到指定的市场文件夹中。
     delete的场合：
       业务层从指定的市场文件夹中删除文件。

    4.6 市场列表获取核心验证逻辑：
     初期表示的场合：
       • 检查数据访问权限
       • 验证数据完整性
     upload的场合：
       • 检查文件类型是否符合要求
       • 验证文件大小是否超出限制
       • 确认目标市场目录存在
     delete的场合：
       • 检查目标市场目录是否存在
       • 验证文件是否存在
       • 确认文件路径安全

    4.7 封装【UD12UploadDeletetemplatResponse】响应对象，统一返回
    初期表示的场合：
    {
    "code": 200,
    "msg": "查询成功",
    "data": {
        "markets": [
          {
            "market": "JPN"
          }
        ]
      }
    }
    upload的场合：
    {
    "code": 200,
    "msg": "TEMPLATE [ファイル名] WAS SUCESSFULLY UPLOADED TO MARKET [市場コード]",
    "data": null
    }
    delete的场合：
    {
    "code": 200,
    "msg": "TEMPLATE [ファイル名] WAS SUCESSFULLY DELETED FROM MARKET [市場コード]",
    "data": null
    }
----------------------------------------------------------------------------------------------------------------
    【UD14SearchresultistApi】
    4.1 客户端通过POST请求访问接口 /api/UD14SearchresultistApi/UD14SelectMarketmaster，无需特殊请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD14SearchresultistApi/UD14SelectHdocuserdefinedrules，将市场信息以JSON格式发送至后端服务。

    4.2 后端【UD14SearchresultistController】接收前端请求，通过【UD14SearchresultistRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD14SearchresultistService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【Ud14SearchresultistServiceImpl】业务实现层执行核心搜索逻辑：
      初期表示的场合：
       • 验证请求参数的有效性
       • 校验不通过则直接返回参数异常响应
      选择市场后：
       • 对请求参数进行非空、长度、格式等合法性校验
       • 验证市场代码的有效性
       • 校验不通过则直接返回参数异常响应
    
    4.5 业务层通过【Mapper】数据访问层执行以下操作：
      初期表示的场合：
        业务层通过【MarketMasterMapper】数据访问层，查询数据库中的市场主数据记录。
      选择市场后：
        业务层通过【UserdefinedrulesMapper】数据访问层，根据市场代码查询数据库中的用户定义规则记录。

    4.6 封装【UD14SearchresultistResponse】响应对象，统一返回标准格式：
      初期表示的场合：
      {
        "code": 200,
        "msg": "查询成功",
        "data": {
        "markets": [
          {
            "market": "JPN"
          }
        ]
      }
    }
 
      选择市场后：
      {
        "code": 200,
        "msg": "查询成功",
        "data": {
           "rules": [
            {
              "variable": "VARIABLE_NAME"
            }
          ]
        }
      }

----------------------------------------------------------------------------------------------------------------
    【UD15SelecthdocsenddatavinplateApi】
    4.1 客户端通过GET请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15ViewInfo，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15SetRegenerate，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15SetOK，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15ChangetoBasicInfo，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15ChangetoAdvancedInfo，将chassisNumber请求参数，以JSON格式发送至后端服务。

    4.2 后端【UD15SelecthdocsenddatavinplateController】接收前端请求，通过【UD15SelecthdocsenddatavinplateRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。
    
    4.3 控制器层调用【UD15SelecthdocsenddatavinplateService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【Ud15SelecthdocsenddatavinplateServiceImpl】业务实现层执行核心逻辑：
      • 对请求参数进行非空、长度、格式等合法性校验
      • 对chassisNumber格式、存在状态进行前置校验
      • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【HdocSendDataVinPlateMapper】数据访问层，根据chassisNumber操作数据库，完成业务逻辑处理。

    4.6 VIN Plate核心验证逻辑：
    • 判断VIN Plate是否存在，不存在则返回"Chassis number XXX not found."
    • 更新对应底盘的STATUS字段和TYPE字段
    • 操作失败则返回相应错误信息

    4.7 封装【UD15SelecthdocsenddatavinplateResponse】响应对象，统一返回标准格式：
    View Info的场合：
    {
        "code": 200,
        "msg": "查询成功",
        "data": {
            "chassisNumber": "string",
            "type": "number", 
            "status": "number",
            "message": "string",
            "registerDatetime": "datetime",
            "docReady": "boolean",
            "docSent": "boolean",
            "xmlDoc": {
              "printItemName": "array",
              "variants": "object"
            }
        }
    }
    其他场合
    {
        "code": 200,
        "msg": "状态已更新为完成",
        "data": null
    }

----------------------------------------------------------------------------------------------------------------
    【UD15SelecthdocsenddatavinplateApi】
    4.1 客户端通过GET请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15ViewInfo，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15SetRegenerate，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15SetOK，将chassisNumber请求参数，以JSON格式发送至后端服务。
        客户端通过POST请求访问接口 /api/UD15SelecthdocsenddatavinplateApi/UD15ChangetoBasicInfo，将chassisNumber请求参数，以JSON格式发送至后端服务。

    4.2 后端【UD15SelecthdocsenddatavinplateController】接收前端请求，通过【UD15SelecthdocsenddatavinplateRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。
    
    4.3 控制器层调用【UD15SelecthdocsenddatavinplateService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【Ud15SelecthdocsenddatavinplateServiceImpl】业务实现层执行核心逻辑：
      • 对请求参数进行非空、长度、格式等合法性校验
      • 对chassisNumber格式、存在状态进行前置校验
      • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【HdocSendDataVinPlateMapper】数据访问层，根据chassisNumber操作数据库，完成业务逻辑处理。

    4.6 VIN Plate核心验证逻辑：
    • 判断VIN Plate是否存在，不存在则返回"Chassis number XXX not found."
    • 更新对应底盘的STATUS字段和TYPE字段
    • 操作失败则返回相应错误信息

    4.7 封装【UD15SelecthdocsenddatavinplateResponse】响应对象，统一返回标准格式：
    View Info的场合：
    {
        "code": 200,
        "msg": "查询成功",
        "data": {
            "chassisNumber": "string",
            "type": "number", 
            "status": "number",
            "message": "string",
            "registerDatetime": "datetime",
            "docReady": "boolean",
            "docSent": "boolean",
            "xmlDoc": {
              "printItemName": "array",
              "variants": "object"
            }
        }
    }
    其他场合
    {
        "code": 200,
        "msg": "状态已更新为完成",
        "data": null
    }

----------------------------------------------------------------------------------------------------------------
   【UD16ADChangeApi】
    4.1 检查的场合：客户端通过POST请求访问接口 /api/UD16ADChangeApi/UD16SelectHdocAdcaChange，将serie_chnr、desc请求参数，以JSON格式发送至后端服务。
                追加的场合：客户端通过POST请求访问接口 /api/UD16ADChangeApi/UD16InsertHdocAdcaChange，将serie_chnr、desc请求参数，以JSON格式发送至后端服务。
                删除的场合：客户端通过POST请求访问接口 /api/UD16ADChangeApi/UD16UpdateHdocAdcaChange，将serie_chnr、desc请求参数，以JSON格式发送至后端服务。

    4.2 后端【UD16ADChangeController】接收前端请求，通过【UD16ADChangeRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD16ADChangeService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD16ADChangeServiceImpl】业务实现层执行核心登录逻辑：
             检查的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 根据入力参数，检索数据库中是否存在数据
             追加的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 根据入力参数，检索数据库中是否存在数据
        • 不存在的场合，根据请求参数，登录数据库
             删除的场合：
        • 对请求参数进行非空、长度、格式等合法性校验
        • 根据入力参数，检索数据库中是否存在数据
        • 如果存在，对数据进行删除

    4.5 业务层通过【***Mapper】数据访问层，根据入力参数查询数据库中的信息，再根据不同的功能，对数据库进行追加和删除。

    4.6 核心验证逻辑：
        • 判断数据库中（ADCA Changeテーブル）的Serie-Chnr数据是否存在
        • 根据前台调用不同的借口，进行检索，登录和删除处理
        • 在进行追加和删除操作的场合，需要先检索数据库中是否存在该数据

    4.7 验证全部通过后：
             检查的场合：
        • 返回检查通过的标识
             追加的场合：
        • 返回登录成功的标识
             删除的场合：
        • 返回删除成功的标识

    4.8 封装【UD16ADChangeResponse】响应对象，统一返回标准格式：
             检查的场合：
        {
            "code": 200,
            "msg": "没有错误",
            "data": {
                ······
            }
        }
             追加的场合：
        {
            "code": 200,
            "msg": "追加成功",
            "data": {
                ······
            }
        }
             删除的场合：
        {
            "code": 200,
            "msg": "删除成功",
            "data": {
                ······
            }
        }
----------------------------------------------------------------------------------------------------------------
   【UD17HDocUserAdministration​​Api】
    4.1 客户端通过 POST 请求访问接口 `/api/UD17HDocUserAdministration​​Api/UD17Userinfo`，将用户ID（userid）请求参数，以 JSON 格式发送至后端服务。
        客户端通过 POST 请求访问接口 `/api/UD17HDocUserAdministration​​Api/UD17UpdateRole`，将用户ID（userid）和权限信息（permissions）请求参数，以 JSON 格式发送至后端服务。
        客户端通过 POST 请求访问接口 `/api/UD17HDocUserAdministration​​Api/UD17DeleteRole`，将用户ID（userid）请求参数，以 JSON 格式发送至后端服务。

    4.2 后端【UD17HDocUserAdministrationController】接收前端请求，通过【UD17HDocUserAdministrationRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UD17HDocUserAdministrationService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD17HDocUserAdministrationServiceImpl】业务实现层执行核心查询逻辑：
      • 对请求参数进行非空、长度、格式等合法性校验
      • 对userid格式、存在状态进行前置校验
      • 校验不通过则直接返回参数异常响应

    4.5 业务层通过【UserPermissionMapper】数据访问层，根据userid查询数据库中的用户权限信息，获取用户权限实体对象。

    4.6 用户权限管理核心验证逻辑：
    userinfo的场合：
      • 判断用户是否存在，不存在则返回"We didn't recognize the userid you entered. Please try again."
      • 从用户功能权限表读取该用户已有权限
      • 查询失败则返回相应错误信息
    update-role的场合：
      • 判断用户是否存在，不存在则返回"We didn't recognize the userid you entered. Please try again."
      • 更新对应用户的权限信息
      • 更新失败则返回相应错误信息
    delete-role的场合：
      • 清空该用户名下全部权限
      • 删除失败则返回相应错误信息

    4.7 验证全部通过后：
    userinfo的场合：
      • 组装查询结果（Username, 权限列表等）
      • 构建查询成功响应体，包含用户权限信息
    update-role的场合：
      • 更新数据库中对应的用户权限记录
      • 记录操作日志（操作时间、操作类型、userid）
      • 构建更新成功响应体，包含成功信息
    delete-role的场合：
      • 删除数据库中对应的用户权限记录
      • 记录操作日志（操作时间、操作类型、userid）
      • 构建删除成功响应体，包含成功信息

----------------------------------------------------------------------------------------------------------------
【UD18HDocUserDocAdministrationApi】
  4.1 客户端通过 GET 请求访问接口 `/api/user-doc-admin/document-list`，无请求参数，获取全部文档信息。
      客户端通过 POST 请求访问接口 `/api/user-doc-admin/select-user-doc`，将用户ID（userid）请求参数，以 JSON 格式发送至后端服务。
      客户端通过 POST 请求访问接口 `/api/user-doc-admin/update-user-doc`，将用户ID（userid）和文档信息（documents）请求参数，以 JSON 格式发送至后端服务。

  4.2 后端【UD18HDocUserDocAdministrationController】接收前端请求，完成请求入口层的参数接收。

  4.3 控制器层调用【UD18HDocUserDocAdministrationService】接口中的方法，将请求转发至业务逻辑层处理。

  4.4 【UD18HDocUserDocAdministrationServiceImpl】业务实现层执行核心查询逻辑：
  初期表示的场合：
    • 对业务场景进行前置校验（如确认当前用户是否有权限查看文档类型字典）
    • 准备查询条件（本接口为全量获取，无额外过滤条件）
  User Info的场合：
    • 对请求参数进行非空、长度、格式等合法性校验
    • 对userid格式、存在状态进行前置校验
    • 校验不通过则直接返回参数异常响应
  Update的场合：
    • 对请求参数进行非空、长度、格式等合法性校验
    • 对userid格式、存在状态进行前置校验
    • 校验不通过则直接返回参数异常响应

  4.5 
  初期表示的场合：
    业务层通过【HdocDocumentListMapper】数据访问层，执行 SQL 查询，从 `HDOC_DOCUMENT_LIST` 表中检索数据。
  User Info的场合：
    业务层通过【UserDocumentPermissionMapper】数据访问层，根据userid查询数据库中的用户文档权限信息，获取用户文档权限实体对象。
  Update的场合：
    业务层通过【UserDocumentPermissionMapper】数据访问层，根据userid和documents查询数据库中的用户文档权限信息，获取用户文档权限实体对象。
  
  4.6 数据获取与处理逻辑：
  初期表示的场合
    • 查询 `HDOC_DOCUMENT_LIST` 表中所有的 `DOCTYPE` 和 `DESCRIPTION` 字段值
    • （可选优化）若存在重复值，进行去重处理（Distinct）
    • 若表中无数据，返回空列表
  User Info的场合：
    • 判断用户是否存在，不存在则返回"We didn't recognize the userid you entered. Please try again."
    • 从用户功能权限表读取该用户已有的文档权限
    • 查询失败则返回相应错误信息
  Update的场合：
    • 判断用户是否存在，不存在则返回"We didn't recognize the userid you entered. Please try again."
    • 删除匹配的用户文档权限数据
    • 删除失败则返回相应错误信息

  4.7 验证全部通过后：
  初期表示的场合
    • 将查询到的 `DOCTYPE` 和 `DESCRIPTION` 集合封装为列表结构
    • 构建查询成功响应体，包含文档类型列表数据
  User Info的场合：
    • 组装查询结果（Document类型，Description等）
    • 构建查询成功响应体，包含用户文档权限信息
  Update的场合：
    • 向数据库中插入新的用户文档权限记录
    • 记录操作日志（操作时间、操作类型、userid）
    • 构建创建成功响应体，包含成功信息

  4.8 封装【UD18HDocUserDocAdministrationResponse】响应对象，统一返回标准格式：
  初期表示的场合
    {
        "code": 200,
        "msg": "获取成功",
        "data": {
            "documents": [
              {
                "doctype": "string",
                "description": "string"
              }
            ]
        }
    }
  User Info的场合：
     {
        "code": 200,
        "msg": "获取成功",
        "data": {
            "documents": [
              {
                "doctype": "string",
                "description": "string"
              }
            ]
        }
    }
  Update的场合：
    {
        "code": 200,
        "msg": "更新成功",
        "data": null
    }

----------------------------------------------------------------------------------------------------------------
  根据模块组成部分结构生成【UD19SearchResultListApi】相关代码
  4.1 客户端通过 POST 请求访问接口 `/api/UD19SearchResultListApi/search`，将用户ID（userid）、用户名（user）、市场（market）、未配置（notSet）、规则（rule）、模板（template）等请求参数，以 JSON 格式发送至后端服务。

  4.2 后端【UD19SearchResultListController】接收前端请求，通过【UD19SearchResultListRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

  4.3 控制器层调用【UD19SearchResultListService】接口中的 `searchHdoc()` 方法，将校验后的请求对象传递至业务逻辑层处理。

  4.4 【UD19SearchResultListServiceImpl】业务实现层执行核心查询逻辑：
    • 对请求参数进行非空、长度、格式等合法性校验
    • 对userid、user格式、存在状态进行前置校验
    • 校验不通过则直接返回参数异常响应

  4.5 业务层通过【UserSearchMapper】数据访问层，根据userid或user查询数据库中的用户信息，获取用户实体对象。

  4.6 用户搜索核心验证逻辑：
    • 输入Userid后点击【Search】，从Saviynt获取用户名，再从用户市场权限表带出用户绑定的市场信息
    • 输入用户名后点击【Search】，先从Saviynt反查用户ID，再依据用户ID从用户市场权限表获取所属市场
    • 勾选未配置(Not set)选项并点击【Search】，检索全部用户
    • 勾选规则(Rule)选项并点击【Search】，筛选拥有规则管理员权限的用户
    • 勾选模板(Template)选项并点击【Search】，筛选拥有模板管理员权限的用户
    • 查询失败则返回相应错误信息

  4.7 验证全部通过后：
    • 组装查询结果（Userid, Username, Markets等）
    • 构建查询成功响应体，包含用户及市场权限信息

  4.8 封装【UD19SearchResultListResponse】响应对象，统一返回标准格式：
    {
        "code": 200,
        "msg": "查询成功",
        "data": {
            "users": [
              {
                "userid": "string",
                "username": "string",
                "markets": ["string"]
              }
            ],
            "count": 0
        }
    }
----------------------------------------------------------------------------------------------------------------
【UD20GetDocumentListApi】
  4.1 客户端通过 GET 请求访问接口 `/api/market-document-settings-list/document-list`，从前画面传入的参数，以 JSON 格式发送至后端服务。

  4.2 后端【UD20GetDocumentListController】接收前端请求，通过【UD20GetDocumentListRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

  4.3 控制器层调用【UD20GetDocumentListService】接口中的 `selectHdocDocumentList()` 方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UD20GetDocumentListServiceImpl】业务实现层执行核心查询逻辑：
    • 对请求参数进行非空、长度、格式等合法性校验
    • 对参数格式、存在状态进行前置校验
    • 校验不通过则直接返回参数异常响应

  4.5 业务层通过【DocumentListMapper】数据访问层，根据传入参数查询数据库中的文档信息，获取文档实体对象。

  4.6 文档列表核心验证逻辑：
    • 根据前画面传入的参数，获取文档一览情报
    • 查询失败则返回相应错误信息

  4.7 验证全部通过后：
    • 组装查询结果（Document type, Business unit, User, Date等）
    • 构建查询成功响应体，包含文档信息

  4.8 封装【UD20GetDocumentListResponse】响应对象，统一返回标准格式：
    {
        "code": 200,
        "msg": "查询成功",
        "data": {
            "documents": [
              {
                "doctype": "string",
                "registerUser": "string",
                "registerDatetime": "string"
              }
            ]
        }
    }

  【UD20GetDocumentListApi】
  4.1 客户端通过 POST 请求访问接口 `/api/market-document-settings/update`，将文档类型（documentType）、业务单元（businessUnit）、用户（user）、日期（date）等请求参数，以 JSON 格式发送至后端服务。

  4.2 后端【UD20MarketDocumentSettingsController】接收前端请求，通过【UD20MarketDocumentSettingsRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

  4.3 控制器层调用【UD20MarketDocumentSettingsService】接口中的 `updateHdocDocumentList()` 方法，将校验后的请求对象传递至业务逻辑层处理。

  4.4 【UD20MarketDocumentSettingsServiceImpl】业务实现层执行核心更新逻辑：
    • 对请求参数进行非空、长度、格式等合法性校验
    • 对documentType格式、存在状态进行前置校验
    • 校验不通过则直接返回参数异常响应

  4.5 业务层通过【HdocDocumentListMapper】数据访问层，根据documentType查询数据库中的文档信息，获取文档实体对象。

  4.6 文档设置核心验证逻辑：
    • 判断文档是否存在，不存在则返回"No data found"
    • 更新对应文档的设置信息
    • 更新失败则返回相应错误信息

  4.7 验证全部通过后：
    • 组装查询结果（DocumentType, BusinessUnit, User, Date等）
    • 构建更新成功响应体，包含文档设置信息

  4.8 封装【UD20MarketDocumentSettingsResponse】响应对象，统一返回标准格式：
    {
        "code": 200,
        "msg": "更新成功",
        "data": {
            "documentType": "string",
            "businessUnit": "string",
            "user": "string",
            "date": "string"
        }
    }

----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
        
    *************根据模块组成部分结构生成【UD12UploadDeleteTemplateApi】相关代码
    4.1 客户端通过POST请求访问接口 /api/market/upload，将Template File、Market等请求参数，以JSON格式发送至后端服务。

    4.2 后端【UploadDeleteTemplateController】接收前端请求，通过【UploadDeleteTemplateRequest】实体对象封装并校验请求参数，完成请求入口层的参数接收与初步格式验证。

    4.3 控制器层调用【UploadDeleteTemplateService】接口中的方法，将校验后的请求对象传递至业务逻辑层处理。

    4.4 【UploadDeleteTemplateServiceImpl】业务实现层执行核心登录逻辑：
        • 对Template File文件进行大小检证
        • 对Market进行前置校验
        • 校验不通过则直接返回参数异常响应

    4.5 业务层通过后将文件上传到服务器端，并返回文件上传结果，服务器地址为https://172.17.0.63/F:/hdoc/template/upload

    4.8 封装【UploadDeleteTemplateResponse】响应对象，统一返回标准格式：
        {
            "code": 200,
            "msg": "上传成功",
            "data": {
                "XXX"："XXXClass",
                "YYYY"："YYYYList",
                ...
            }
        }

    ............（TODO：请根据API一览和概要设计描述所有API）

5. 数据库设计（TODO：请根据API一览和概要设计描述各个API的SQL）
    【AuthenticationApi】
    5.1 - 查询语句: 
    SELECT USERID, PASSWORD, USERNAME FROM hdoc_user_infor WHERE USERID = {userId} AND PASSWORD = {password}
----------------------------------------------------------------------------------------------------------------
    【UD03SelectHdocdocumentlistApi】
    5.2 - 查询语句: 
    SELECT DOCTYPE FROM HDOC_DOCUMENT_LIST
----------------------------------------------------------------------------------------------------------------
    【UD04SelectGeneratedocumentApi】
    5.3 - 查询语句: 
        select 
         OM.ORDERNUMBER,
         OM.BUILD,
         OM.SPEC,
         OM.CUSTOMER_ADAP as sNoteNO,
         GENERAL.COUNTRY_OF_OPERATION as market,
         MASTER.LOAD_INDEX as loadIndex,
         CHANGE.ACT,
         MODIFICATION.VARIABLE,
         MODIFICATION.NEWVAL
        form 
          HDOC_REC_DATA_OM OM
        INNER JOIN HDOC_REC_DATA_VDA_GENERAL GENERAL
          ON OM.SERIE = GENERAL.SERIE
          AND OM.CHNR = GENERAL.CHNR
        INNER JOIN HDOC_REC_DATA_KOLA_TIRE_MASTER MASTER
          ON GENERAL.BRAND_ID = MASTER.BRAND
        INNER JOIN HDOC_ADCA_CHANGE CHANGE
          ON OM.SERIE = CHANGE.SERIE
          AND OM.CHNR = CHANGE.CHNR
        INNER JOIN HDOC_ADCA_MODIFICATION
          ON OM.SERIE = MODIFICATION.SERIE
          AND OM.CHNR = MODIFICATION.CHNR
        WHERE
         OM.CHASSIS_NO = :Chassis_no
         AND OM.ORDERNUMBER = :Ordernumber
         AND OM.MARKET = :Market
----------------------------------------------------------------------------------------------------------------
    【UD05ModifyDocumentApi】
    5.4 - 查询语句: 
    SELECT a.NEWVAL, b.DESCRIPTION FROM HDOC_ADCA_MODIFICATION a Left join HDOC_VARIABLES b on a.VARIABLE = b.VARIABLE WHERE SERIE = SERIE AND CHNO = CHNO
        - 更新语句: 
    UPDATE SET NEWVAL = modifiedValue FORM HDOC_ADCA_MODIFICATION WHERE SERIE = SERIE AND CHNO = CHNO AND DESCRIPTION = Description
----------------------------------------------------------------------------------------------------------------
    【UD06SaveModificationsApi】
    5.5 - 查询语句: 
    SELECT * FROM HDOC_ADCA_MODIFICATION WHERE SERIE = ChassisSerie AND CHNO = ChassisNumber
----------------------------------------------------------------------------------------------------------------
    【UD07VehicleSpecificationApi】
    5.6 - 查询语句: 
      SELECT
        OM.MODEL,
        OM.BUILD,
        GENERAL.PRODUCT_TYPE,
        GENERAL.VIN,
        KOLA.SYMBOL,
        GENERAL.COUNTRY_OF_OPERATION,
        VDA.FAMILY_ID,
        VDA.VARIANT_ID,
        OM.CUSTOMER_ADAP
      FROM
        HDOC_REC_DATA_VDA_GENERAL GENERAL
      INNER JOIN
        HDOC_REC_DATA_OM OM
      ON
          OM.SERIE = GENERAL.SERIE
      AND OM.CHNR = GENERAL.CHNR
      INNER JOIN
        HDOC_REC_DATA_VDA_VARIANTS VDA
      ON
          GENERAL.SERIE = VDA.SERIE
      AND GENERAL.CHNR = VDA.CHNR
      INNER JOIN
        HDOC_REC_DATA_KOLA_VARIANT KOLA
      ON
          VDA.FAMILY_ID = KOLA.FAMILY_ID
      AND VDA.VARIANT_ID = KOLA.VARIANT_ID
      WHERE
        GENERAL.SERIE = {serie}
        AND GENERAL.CHNR = {number}

      SELECT
        LPAD(SUBSTRING(KOLA.SYMBOL, 1, 8), 8, ' ') AS SYMBOL_STR,
        KOLA.FUNCTION_GROUP,
        KOLA.DESCRIPTION
      FROM
        HDOC_REC_DATA_KOLA_VARIANT KOLA
      WHERE
        KOLA.FAMILY_ID = {family_id}
        AND KOLA.VARIANT_ID = {variant_id}
      ORDER BY
          CONCAT(LPAD(KOLA.FUNCTION_GROUP, 4, ' '), LPAD(VDA.FAMILY_ID, 3, ' '))
----------------------------------------------------------------------------------------------------------------
    【UD08HomologationVariablesApi】
    5.7 
        - UD08SelectProductclassmaster的查询语句: 
    SELECT * FROM PRODUCT_CLASS_MASTER

        - UD08SelectMarketmaster的查询语句: 
    SELECT * FROM MARKET_MASTER

        -  UD08SelectHdocvariables的查询语句：
     SELECT VARIABLE FROM HDOC_VARIABLES

        - UD08Add追加语句:
       INSERT INTO HDOC_USER_DEFINED_RULES (
          PC,
          NUM,
          MARKET,
          VARIABLE,			
          VAL,			
          VS,			
          VS2,			
          COMMENTS,			
          ADD_DATE,	
          DELETE_DATE,		
          REGISTER_USER,			
          REGISTER_DATETIME			
       )
      VALUES(
          #{Product class},
          #{Number},
          #{Market},
          #{Variable},
          #{Value},
          #{string1},
          #{string2},
          #{Comments},
          #{Add},
          #{Delete},
          #{Created by user},
          #{Date},
          #{Search},
          #{Clear},
          #{Add},
          #{Update},
          #{Delete},
          #{user},
          #{Date}
       )
        - UD08Update更新语句:
       UPDATE HDOC_USER_DEFINED_RULES
       SET
          VARIABLE  =  #{Variable},			
          VAL  =  #{Value},			
          VS  =  #{string1},			
          VS2  =  #{string2},			
          COMMENTS  =  #{Comments},			
          ADD_DATE  =  #{Add},			
          DELETE_DATE  = #{Delete},			
          UPDATE_USER  = #{user},			
          UPDATE_DATETIME  =  #{Date}
       WHERE
          PC = #{productClass}
          AND NUM = #{number}
          AND MARKET = #{market}
        - UD08Delete的删除语句:
       DELETE FROM HDOC_USER_DEFINED_RULES
       WHERE
          PC = #{productClass}
          AND NUM = #{number}
          AND MARKET = #{market}

----------------------------------------------------------------------------------------------------------------
    【UD09DeleteHdocuserdefinedrulesApi】
        5.8 查询语句:
       SELECT
        PC,
        NUM,
        MARKET,
        VARIABLE,			
        VAL,			
        VS,			
        VS2,			
        COMMENTS,			
        ADD_DATE,
        DELETE_DATE,			
        REGISTER_USER,			
        REGISTER_DATETIME,
        COUNT(1)
       FROM HDOC_USER_DEFINED_RULES
       WHERE
        1=1
        AND (PC  = {productClass} OR {productClass} IS NULL)
        AND (NUM  = {number} OR {number} IS NULL)
        AND (MARKET  = {market} OR {market} IS NULL)
        AND (VARIABLE  LIKE CONCAT('%' ,{Variable},'%') OR {Variable} IS NULL)		
        AND (VAL  LIKE CONCAT('%' ,{Value},'%') OR {Value} IS NULL)			
        AND (VS   LIKE CONCAT('%' ,{string1},'%') OR {string1} IS NULL)
        AND (VS2  LIKE CONCAT('%' ,{string2},'%') OR {string2} IS NULL)	
        AND (COMMENTS  LIKE CONCAT('%' ,{Comments},'%') OR {Comments} IS NULL)
       GROUP BY
        PC, NUM, MARKET, VARIABLE, VAL, VS, VS2, COMMENTS, ADD_DATE, DELETE_DATE, REGISTER_USER,REGISTER_DATETIME;

        删除语句：
       DELETE FROM HDOC_USER_DEFINED_RULES
       WHERE
        PC  = {productClass}
        AND NUM  = {number}
        AND MARKET  = {market}
----------------------------------------------------------------------------------------------------------------
    【UD10HdocvariablesApi】
    5.9 
        - 登陆的场合: 
        INSERT INTO HDOC_VARIABLES (
            VARIABLE,        -- 変数コード (必須)
            TYPE,            -- タイプ
            DESCRIPTION,     -- 説明
            CREATED_BY_USER, -- 作成ユーザー
            CREATE_DATE      -- 作成日付
        ) VALUES (
            Variable,   
            Type,   
            Description,
            Created_by_user,
            SYSDATE        );

        - 更新的场合: 
        UPDATE HDOC_VARIABLES
        SET
            TYPE = Type,
            DESCRIPTION = Description,
            CREATED_BY_USER = Created_by_user,
            CREATE_DATE = SYSDATE
        WHERE
            VARIABLE = Variable; 

        - 删除的场合: 
        DELETE FROM HDOC_VARIABLES
        WHERE VARIABLE = Variable;
----------------------------------------------------------------------------------------------------------------
    【UD11HdocvariablesApi】
     5.10  查询语句:
    SELECT
        VARIABLE,
        TYPE,
        DESCRIPTION,
        REGISTER_USER,
        REGISTER_DATETIME
    FROM HDOC_VARIABLES
    WHERE
        1=1
        AND (VARIABLE  LIKE CONCAT('%' ,{Variable},'%') OR {Variable} IS NULL)
        AND (TYPE  LIKE CONCAT('%' ,{Type},'%') OR {Type} IS NULL)
        AND (DESCRIPTION  LIKE CONCAT('%' ,{Description},'%') OR {Description} IS NULL)
----------------------------------------------------------------------------------------------------------------
    【UD14SearchresultistApi】
    5.12:
    SELECT
        VARIABLE
    FROM HDOC_USER_DEFINED_RULES
    WHERE
        MARKET  = {Martet} 
----------------------------------------------------------------------------------------------------------------
    【UD15SelecthdocsenddatavinplateApi】
       5.13:
    View Info的场合：
    - 查询语句:
    SELECT
        TYPE,
        STATUS,
        MSG,
        REGISTER_DATETIME,
        DOC_READY,
        DOC_SENT,
        XML_DOC
    FROM HDOC_SEND_DATA_VIN_PLATE
    WHERE
        SERIE  = {serie}
    AND CHNR   = {no}

    Set Regenerate的场合：
    UPDATE HDOC_SEND_DATA_VIN_PLATE
    SET
        STATUS  =  0
    WHERE
        SERIE  = {serie}
    AND CHNR   = {no}

    Set OK的场合：
    UPDATE HDOC_SEND_DATA_VIN_PLATE
    SET
        STATUS  =  1
    WHERE
        SERIE  = {serie}
    AND CHNR   = {no}

    Change to Basic Info的场合：
    UPDATE HDOC_SEND_DATA_VIN_PLATE
    SET
        STATUS  =  0,
        TYPE  =  '1'
    WHERE
        SERIE  = {serie}
    AND CHNR   = {no}

    Change to Advanced Info的场合：
    UPDATE HDOC_SEND_DATA_VIN_PLATE
    SET
        STATUS  =  0,
        TYPE  =  '2'
    WHERE
        SERIE  = {serie}
    AND CHNR   = {no}


----------------------------------------------------------------------------------------------------------------
   【UD16ADChangeApi】
  5.14：
    UD16SelectHdocAdcaChange的场合(检索)：
         SELECT * FROM HDOC_ADCA_CHANGE WHERE SERIE = serie_chnr AND CHNR = desc

    UD16InsertHdocAdcaChange的场合(追加)：
         检索：SELECT * FROM HDOC_ADCA_CHANGE WHERE SERIE = serie_chnr AND CHNR = desc
         登录：INSERT INTO HDOC_ADCA_CHANGE （SERIE
                       CHNR
                       ACT
                       BU
                       REASON
                       REGISTER_DATETIME
                       REGISTER_USER
                       REGISTER_PROCESS
                       UPDATE_DATETIME
                       UPDATE_USER
                       UPDATE_PROCESS
                       ）VALUES(
                       入力参数serie_chnr,
                       入力参数desc,
                       'Y',
                       '',
                       BU,
                       系统时间,
                       登录者,
                       登录者,
                       系统时间,
                       登录者,
                       登录者
                       ）
    UD16UpdateHdocAdcaChange的场合(删除)：
         检索：SELECT * FROM HDOC_ADCA_CHANGE WHERE SERIE = serie_chnr AND CHNR = desc
         更新：UPDATE HDOC_ADCA_CHANGE SET ACT = 'N' WHERE SERIE = serie_chnr AND CHNR = desc

----------------------------------------------------------------------------------------------------------------
  【UD17HDocUserAdministration​​Api】
   5.15
  - 查询语句:
    SELECT COUNT(1) FROM hdoc_user_infor WHERE USERID = {userid}

    SELECT
     FUNCTIONAUTH.FUNCTION,
     MARKETAUTH.MARKET
    FROM HDOC_FUNCTION_AUTH FUNCTIONAUTH
    LEFT JOIN
     HDOC_MARKET_AUTH MARKETAUTH
   ON  FUNCTIONAUTH.USERID = MARKETAUTH.USERID
   AND FUNCTIONAUTH.FUNCTION = MARKETAUTH.TYPE
   WHERE FUNCTIONAUTH.USERID = {userid}

  - 更新语句: 
    UPDATE  HDOC_FUNCTION_AUTH
    SET FUNCTION = {permissions}
    WHERE USERID = {userid}

    UPDATE  HDOC_MARKET_AUTH
    SET MARKET = {market}
    WHERE USERID = {userid}
    AND   TYPE = {permissions}

  - 删除语句:
    UPDATE  HDOC_FUNCTION_AUTH
    SET FUNCTION = ''
    WHERE USERID = {userid}

    UPDATE  HDOC_MARKET_AUTH
    SET MARKET = ''
    WHERE USERID = {userid}

----------------------------------------------------------------------------------------------------------------
  【UD18HDocUserDocAdministrationApi】
   5.16：
  - 查询语句:SELECT DOCTYPE,DESCRIPTION FROM HDOC_DOCUMENT_LIST
   SELECT
    DOCUMENTLIST.DOCTYPE,
    DOCUMENTLIST.DESCRIPTION 
   FROM  HDOC_USER_DOC USERDOC
   INNER JOIN  HDOC_DOCUMENT_LIST DOCUMENTLIST
   ON USERDOC.DOCTYPE = DOCUMENTLIST.DOCTYPE
   WHERE USERDOC.USERID = {UserID}

  - 更新语句: DELETE FROM HDOC_USER_DOC WHERE USERID = {UserID}
   INSERT INTO HDOC_USER_DOC
   (USERID,
    DOCTYPE,
    REGISTER_DATETIME,
    REGISTER_USER
   )VALUES(
    {UserID}，
    {Document},
    {User},
    NOW()
  )

----------------------------------------------------------------------------------------------------------------
  【UD19SearchResultListApi】
  - 查询语句:
  SELECT
   USERINFO.USERID,
   USERINFO.USERNAME,
   MARKETAUTH.MARKET
  FROM HDOC_USER_INFOR USERINFO
  INNER JOIN HDOC_MARKET_AUTH MARKETAUTH
  ON USERINFO.USERID = MARKETAUTH.USERID
  WHERE USERINFO.USERID = ({userId} OR {userId} IS NULL)
  AND   USERINFO.USERNAME = ({username} OR {username} IS NULL)
  AND  MARKETAUTH.TYPE = ({type} OR {type}IS NULL)
----------------------------------------------------------------------------------------------------------------
  【UD20GetDocumentListApi】
  - 查询语句:
  SELECT
   DOCTYPE,
   DESCRIPTION,
   REGISTER_USER,
   REGISTER_DATETIME
  FROM HDOC_DOCUMENT_LIST
  WHERE (DOCTYPE LIKE CONCAT('%' ,{DocumentType},'%') OR {DocumentType} IS NULL)

  【UD20MarketDocumentSettingsApi】
  - 更新语句:SELECT COUNT(1) FROM HDOC_DOCUMENT_LIST WHERE DOCTYPE = {documentType}
            UPDATE HDOC_DOCUMENT_LIST SET REGISTER_USER = {user}, REGISTER_DATETIME = {date} WHERE DOCTYPE = {documentType}
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------



6. 开发指导原则

6.1 代码编写规范
    - 遵循MVC三层架构模式
    - 使用Lombok减少样板代码
    - 保持Controller、Service、Mapper层职责分离

6.2 错误处理
    - 对输入参数进行验证
    - 合理捕获和处理异常
    - 返回有意义的错误信息
