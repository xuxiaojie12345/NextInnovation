根据文件 `内部設計模板.md` 的结构规范，结合文件 `プロンプトUD03.txt` 中的具体业务需求，生成的详细设计文档如下：

---

# 认证文档生成查询模块 (Document Generation Query Module) 详细设计说明书

| 文档编号     | UD03                             | 版本号     | v1.0       |
| :----------- | :------------------------------- | :--------- | :--------- |
| **模块名称** | 认证文档生成查询 (Doc Gen Query) | **作成日** | 2026-05-27 |
| **作成者**   | AI Assistant                     | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块是认证文档生成流程的入口页面。用户通过输入车辆底盘系列（Chassis series）、底盘编号（Chassis no）和文档类型（Document type），发起文档生成请求。

- **目标**：提供简洁的查询入口，支持自动回填上次检索条件以提升效率，并校验输入合法性后跳转至生成页面。
- **安全性**：仅允许授权用户访问，所有通信需通过 HTTPS 加密，不暴露敏感信息。
- **用户体验**：
  - 界面简洁，仅包含必要输入项。
  - 提供“Help”链接便于查阅说明。
  - 错误信息在页面内明确提示，避免弹窗打断。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| 項目名 (Item)      | 種別 (Type)   | 必須 (Req) | MaxLength |  I/O   | 許容文字 (Allowed Chars)     | 文字配置 (Align) |   初期値 (Default)    | 表示制御 (Control) | 备注                                    |
| :----------------- | :------------ | :--------: | :-------: | :----: | :--------------------------- | :--------------: | :-------------------: | :----------------: | :-------------------------------------- |
| **Chassis series** | TextField     |     Y      |     5     | Input  | 半角英数字 (`a-z, A-Z, 0-9`) |    左 (Left)     |      空 (Empty)       |   活性 (Enabled)   | 示例：JPCT；自动去除首尾空格            |
| **Chassis no**     | TextField     |     Y      |    10     | Input  | 半角数字 (`0-9`)             |    左 (Left)     |      空 (Empty)       |   活性 (Enabled)   | 示例：028321；自动去除首尾空格          |
| **Document type**  | Pull-downList |     Y      |     -     | Input  | 半角英数字+記号              |    左 (Left)     |      空 (Empty)       |   活性 (Enabled)   | 数据源：`UD03SelectHdocdocumentlistApi` |
| **Submit**         | Button        |     -      |     -     | Action | -                            |   中 (Center)    |           -           |   活性 (Enabled)   | 触发校验与跳转                          |
| **Reset**          | Button        |     -      |     -     | Action | -                            |   中 (Center)    |           -           |   活性 (Enabled)   | 清空表单                                |
| **Help**           | Button/Link   |     -      |     -     | Action | -                            |   中 (Center)    |           -           |   活性 (Enabled)   | 跳转至帮助页面                          |
| **Support Mail**   | Label         |     -      |     -     | Output | -                            |    左 (Left)     | support.tpi@volvo.com |   常显 (Visible)   | 固定显示支持邮箱                        |
| **ErrorMessage**   | Label         |     -      |    256    | Output | 任意可见字符                 |    左 (Left)     |      空 (Empty)       | 动态显示 (Dynamic) | 仅在出错时显示红色文字                  |

> **注**：
>
> - **半角英数字**：正则表达式 `[a-zA-Z0-9]`
> - **半角数字**：正则表达式 `[0-9]`
> - **Document Type 数据源**：页面加载时调用接口获取下拉选项。

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

所有校验逻辑均在用户点击 **[Submit]** 按钮时执行。

### 3.1 处理流程

1.  **开始**：用户点击 [Submit] 按钮。
2.  **前置处理**：获取 `Chassis series`, `Chassis no`, `Document type`，执行 `trim()` 去除首尾空白。
3.  **空值校验 (Frontend Check)**：
    - 若 `Chassis series` 为空 **或** `Chassis no` 为空：
      - 设置 `ErrorMessage` = `Chassis series and chassis no are required.`
      - **终止**流程。
    - 若 `Document type` 未选择（值为空或默认占位符）：
      - 设置 `ErrorMessage` = `Please select a document type.`
      - **终止**流程。
4.  **API 调用 (Backend Check)**：
    - 若前端校验通过，调用后端接口验证底盘编号是否存在（隐含逻辑，根据需求描述“若失败则显示 Chassis no is not exists”）。
    - _注：此处假设存在一个验证接口或在跳转后的页面进行验证，根据UD03描述，此处主要做前端校验和获取下拉列表。若需在提交时验证存在性，需调用特定API。根据模板要求，此处主要描述获取下拉列表的初始化逻辑及提交动作。_
    - **修正**：根据需求 5.1.2，页面加载时需调用 `UD03SelectHdocdocumentlistApi` 获取下拉列表。提交时，若后端验证底盘不存在，则返回错误。
5.  **结果处理**：
    - **校验/验证成功**：携带参数跳转至 `Generate document` 页面。
    - **验证失败**：设置 `ErrorMessage` = `Chassis no is not exists`。

### 3.2 校验详细规格表

| No. |   检查时机   |     检查对象      |                检查条件                |          错误消息 (Message Content)           |                动作                 |
| :-: | :----------: | :---------------: | :------------------------------------: | :-------------------------------------------: | :---------------------------------: |
|  1  | Click Submit |  Chassis series   | `Value == null` OR `Trim(Value) == ""` | `Chassis series and chassis no are required.` | 显示 ErrorMessage，焦点停留在该字段 |
|  2  | Click Submit |    Chassis no     | `Value == null` OR `Trim(Value) == ""` | `Chassis series and chassis no are required.` | 显示 ErrorMessage，焦点停留在该字段 |
|  3  | Click Submit |   Document type   |    `Value == null` OR `Value == ""`    |       `Please select a document type.`        | 显示 ErrorMessage，焦点停留在该字段 |
|  4  | API Response | Chassis Existence |  `Status != Success` OR `Code == 404`  |          `Chassis no is not exists`           |          显示 ErrorMessage          |

## 4. 接口定义 (API Specification)

### 4.1 UD03SelectHdocdocumentlistApi

- **功能**：获取文档类型下拉列表数据。
- **Method**: `GET`
- **Endpoint**: `/api/UD03SelectHdocdocumentlistApi/types`

#### Request Body

无参数。

#### Response Success (200 OK)

```json
[
  { "doctyp": "Dimension Plate" },
  { "doctyp": "VIN Plate" },
  { "doctyp": "COC" }
]
```

#### Response Error (500 Internal Server Error)

```json
{
  "error": "Internal Server Error",
  "message": "Failed to load document types"
}
```

_(注：若提交时需要验证底盘存在性，应另有 API，如 `POST /api/chassis/verify`，此处仅列出模板中明确的 API)_

## 5. 异常处理 (Exception Handling)

| 异常场景                 | 处理方式              | Message 显示内容                                 |
| :----------------------- | :-------------------- | :----------------------------------------------- |
| **网络断开/超时**        | 捕获 Network Error    | `Network error. Please check your connection.`   |
| **底盘编号不存在**       | 捕获 API 404/业务错误 | `Chassis no is not exists`                       |
| **服务器内部错误 (500)** | 捕获 Server Error     | `System error. Please contact administrator.`    |
| **获取下拉列表失败**     | 禁用 Submit 或提示    | `Failed to load document types. Please refresh.` |

## 6. 实现注意事项 (Implementation Notes)

1.  **状态管理**：
    - 使用 React State 管理 `chassisSeries`, `chassisNo`, `documentType`, `errorMessage`, `isLoading`, `docTypeOptions`。
    - `ErrorMessage` Label 默认隐藏（内容为空时不占位或高度为0）。
2.  **数据持久化**：
    - 为了支持“自动回填上一次的检索条件”，建议在 `localStorage` 或 `sessionStorage` 中保存用户最后成功提交的查询条件。页面加载时读取并填充表单。
3.  **安全性**：
    - API 请求必须通过 HTTPS 发送。
    - 对输入内容进行 sanitization，防止 XSS 攻击（虽然主要是跳转，但需确保参数编码正确）。
4.  **UI 细节**：
    - [Submit](file://e:\git20260511\NextInnovation\react-ud\src\UD03\UD03.tsx#L10-L10) 按钮在 `isLoading` 期间应设为 `disabled`，防止重复提交。
    - `ErrorMessage` 文字颜色建议使用红色 (`#ff4d4f`) 以起到警示作用。
    - `Reset` 按钮点击后，除了清空表单，也应清除 `localStorage` 中的临时缓存（可选，视产品定义而定，通常 Reset 仅清空当前视图）。
