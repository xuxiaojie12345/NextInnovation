根据文件 `内部設計模板.md` 的结构规范，结合文件 `プロンプトUD04.txt` 中的具体业务需求，生成的详细设计文档如下：

---

# 车辆铭牌生成结果模块 (VIN Plate Generation Result Module) 详细设计说明书

| 文档编号     | UD04                                | 版本号     | v1.0       |
| :----------- | :---------------------------------- | :--------- | :--------- |
| **模块名称** | 车辆铭牌生成结果 (VIN Plate Result) | **作成日** | 2026-06-26 |
| **作成者**   | AI Assistant                        | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块是 VPPS 批处理程序生成车辆铭牌（VIN Plate）后的结果展示页面。用于确认生成结果，展示底盘数据、S-Notes、轮胎主数据、变更状态及最终生成的文档链接，并支持后续操作（如下载、修改、查看详情）。

- **目标**：清晰展示生成结果，提供关键信息的快速访问入口，并通过颜色区分异常状态（如 AD-Change）。
- **安全性**：错误信息不暴露系统内部结构，所有通信需通过 HTTPS 加密。
- **用户体验**：
  - 关键信息（Chassis no, Error message）突出显示。
  - 支持点击链接跳转至详情页或下载文件。
  - 变更状态以红色高亮显示，提示用户注意。
  - 提供清晰的系统时间与版本信息。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| 項目名 (Item)      | 種別 (Type) | 必須 (Req) | MaxLength |  I/O   | 許容文字 (Allowed Chars) | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注                                                                                |
| :----------------- | :---------- | :--------: | :-------: | :----: | :----------------------- | :--------------: | :--------------: | :----------------: | :---------------------------------------------------------------------------------- |
| **Chassis no**     | Label/Link  |     -      |    15     | Output | -                        |    左 (Left)     |        -         |   活性 (Enabled)   | 可点击跳转至底盘详情页                                                              |
| **Ordernumber**    | Label       |     -      |    16     | Output | -                        |    左 (Left)     |        -         |   常显 (Visible)   | -                                                                                   |
| **Build week**     | Label       |     -      |    10     | Output | -                        |    左 (Left)     |        -         |   常显 (Visible)   | -                                                                                   |
| **Spec week**      | Label       |     -      |    10     | Output | -                        |    左 (Left)     |        -         |   常显 (Visible)   | -                                                                                   |
| **Market**         | Label       |     -      |    10     | Output | -                        |    左 (Left)     |        -         |   常显 (Visible)   | -                                                                                   |
| **Master Market**  | Label       |     -      |     -     | Output | -                        |    左 (Left)     |       -EU        |   常显 (Visible)   | 固定值                                                                              |
| **S-Note NO**      | Label       |     -      |   4000    | Output | -                        |    左 (Left)     |        -         | 动态显示 (Dynamic) | 显示 S-Notes 编号列表，若无则隐藏                                                   |
| **S-Note Message** | Label       |     -      |    256    | Output | -                        |    左 (Left)     |        -         | 动态显示 (Dynamic) | 若存在 S-Note，显示红色提示：“The S-Notes above can affect homologation documents.” |
| **Load Index**     | Label       |     -      |    10     | Output | -                        |    左 (Left)     |        -         |   常显 (Visible)   | Tire Master 数据来源                                                                |

| **Modify Doc Link** | Link | - | - | Action | - | 左 (Left) | - | 动态控制 (Dynamic) | AD-Change 启用时显示红色，否则隐藏或禁用 |
| **Using template** | Label | - | 20 | Output | - | 左 (Left) | - | 常显 (Visible) | 显示模板名称 |
| **Replacing parameters** | Label | - | 40 | Output | - | 左 (Left) | - | 常显 (Visible) | 显示被替换参数列表 |
| **Generated document** | Link | - | - | Action | - | 左 (Left) | - | 活性 (Enabled) | 点击下载 .rtf 文件 |
| **Date** | Label | - | 20 | Output | - | 左 (Left) | - | 常显 (Visible) | 当前系统时间 |
| **HDoc version** | Label | - | 20 | Output | - | 左 (Left) | - | 常显 (Visible) | 当前程序版本 |
| **ErrorMessage** | Label | - | - | Output | - | 左 (Left) | - | 动态显示 (Dynamic) | 显示错误信息，若无则为空 |

> **注**：
>
> - **Modify Doc Link**：当 `adChangeEnabled` 为 `true` 时，链接文本颜色设为红色（如 `#ff4d4f`），并允许点击；否则隐藏或置灰。
> - **S-Note Message**：仅当 `sNotes` 数组非空时显示。

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

### 3.1 处理流程

1.  **开始**：用户从上一页面跳转至本页面（携带 Chassis no, Ordernumber, Market 等参数）。
2.  **前置处理**：
    - 获取 URL 参数或 State 中的 `chassisNo`, `ordernumber`, `market`。
    - 执行非空校验。
3.  **空值校验 (Frontend Check)**：
    - 若 `chassisNo` 为空：
      - 设置 `ErrorMessage` = `无法加载数据，请检查输入`
      - **终止**流程，不调用 API。
4.  **API 调用 (Backend Check)**：
    - 若校验通过，调用 `GET /api/UD04SelectGeneratedocumentApi/SelectGeneratedocument`。
5.  **结果处理**：
    - **成功 (200)**：
      - 渲染所有字段。
      - 若 `sNotes` 存在，显示 S-Note 列表及红色警告消息。
      - 若 `adChangeEnabled` 为 true，将 `Modify Doc Link` 标红并激活。
      - 绑定 `Generated document` 下载链接。
    - **失败 (404/500)**：
      - 设置 `ErrorMessage` = `数据加载失败，请稍后重试` 或具体错误消息（如 `Chassis not found`）。

### 3.2 校验详细规格表

| No. |   检查时机   |      检查对象      |                检查条件                |                      错误消息 (Message Content)                       |            动作             |
| :-: | :----------: | :----------------: | :------------------------------------: | :-------------------------------------------------------------------: | :-------------------------: |
|  1  |  Page Mount  |     Chassis no     | `Value == null` OR `Trim(Value) == ""` |                      `无法加载数据，请检查输入`                       | 显示 ErrorMessage，停止加载 |
|  2  | API Response | Template Existence |         `templateName == null`         |             `Can not find template for doctype VIN-PLATE`             |      显示 ErrorMessage      |
|  3  | API Response |     Rule Match     |     `replacedParams` 为空且应有值      |              `No template rule defined for this truck.`               |      显示 ErrorMessage      |
|  4  | API Response |    Batch Status    |          `status == 'FAILED'`          | `Batch processing failed: Failed to make the Inplate Error reson:XXX` |      显示 ErrorMessage      |

## 4. 接口定义 (API Specification)

### 4.1 UD04SelectGeneratedocumentApi

- **功能**：获取指定底盘的车辆铭牌生成结果及相关元数据。
- **Method**: `GET`
- **Endpoint**: `/api/UD04SelectGeneratedocumentApi/SelectGeneratedocument`

#### Request Parameters

- `chassisNo`: String (Path Param)
- `ordernumber`: String (Path Param)
- `market`: String (Path Param)

#### Response Success (200 OK)

```json
{
  "chassisNo": "JPCT_013945",
  "ordernumber": "15101319",
  "buildWeek": "2016173",
  "specWeek": "201617",
  "market": "IDO",
  "masterMarket": "-EU",
  "sNotes": ["S161011", "S1610174"],
  "sNoteMessage": "The S-Notes above can affect homologation documents.",
  "loadIndex": "FTLI-150",
  "speedIndex": "FTSI-K",
  "adChangeEnabled": true,
  "templateName": "_eu/VIN_PLATE_UD_TRUCKS_TSA_INDO_PHIL.rtf",
  "replacedParams": ["AXLE_CONF", "WB_MM"],
  "generatedFileUrl": "/download/vinplate/JPCT_013945.rtf",
  "date": "2022-12-06 07:30:31",
  "hdocVersion": "4.2.1"
}
```

#### Response Error (404 Not Found)

```json
{
  "error": "Not Found",
  "message": "Chassis not found"
}
```

#### Response Error (500 Internal Server Error)

```json
{
  "error": "Internal Server Error",
  "message": "Batch processing failed: Failed to make the Inplate Error reson:XXX"
}
```

## 5. 异常处理 (Exception Handling)

| 异常场景                 | 处理方式           | Message 显示内容                                                      |
| :----------------------- | :----------------- | :-------------------------------------------------------------------- | --- |
| **网络断开/超时**        | 捕获 Network Error | `网络连接失败，请检查网络设置`                                        |
| **模板文件缺失**         | 捕获 API 业务错误  | `无法找到模板文件，请联系管理员`                                      |
| **批处理失败**           | 捕获 API 业务错误  | `Batch processing failed: Failed to make the Inplate Error reson:XXX` |     |
| **服务器内部错误 (500)** | 捕获 Server Error  | `系统繁忙，请稍后再试`                                                |

## 6. 实现注意事项 (Implementation Notes)

1.  **状态管理**：
    - 使用 React State 管理 `vinPlateData`, `errorMessage`, `isLoading`。
    - `ErrorMessage` Label 默认隐藏。
2.  **样式处理**：
    - `Modify Doc Link` 的红色样式建议通过 CSS Class (`class="text-danger"`) 动态绑定。
    - `S-Note Message` 同样使用红色字体以示警示。
3.  **下载功能**：
    - `Generated document` 链接点击时，建议使用 `window.location.href` 或创建隐藏的 `<a>` 标签触发下载，确保文件名正确（后端应在 Header 中设置 `Content-Disposition`）。
4.  **跳转逻辑**：
    - `Chassis no` 点击跳转至 `/chassis-detail/${chassisNo}`。
    - `Modify Doc Link` 点击跳转至修改页面（路径需确认，如 `/modify-doc/${chassisNo}`）。
5.  **安全性**：
    - API 请求必须通过 HTTPS 发送。
    - 对 URL 参数进行编码，防止注入攻击。
