根据文件 `内部設計模板.md` 的结构规范，结合文件 `プロンプトUD18.txt` 中的具体业务需求，生成的详细设计文档如下：

---

# HDoc用户文档权限管理模块 (HDoc User Doc Admin Module) 详细设计说明书

| 文档编号     | UD18                                  | 版本号     | v1.0       |
| :----------- | :------------------------------------ | :--------- | :--------- |
| **模块名称** | HDoc用户文档权限管理 (User Doc Admin) | **作成日** | 2025-04-05 |
| **作成者**   | AI Assistant                          | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块的核心功能是配置用户对各类认证文档的操作权限。用户可通过输入 User ID，从 Saviynt 系统获取用户名，并查询其在 HDoc 中可分配的文档权限列表。系统支持勾选多个文档类型并提交更新，确保权限配置准确且安全。

- **目标**：实现细粒度的文档级权限控制，确保用户仅能访问被授权的文档类型，并与 Saviynt 系统中的用户主数据保持一致。
- **安全性**：
  - 仅允许授权用户（如系统管理员）访问此页面。
  - 所有权限变更需经过后端校验及确认。
  - 数据更新由后端统一控制，防止并发冲突。
  - 所有通信使用 HTTPS 加密。
- **用户体验**：
  - 界面简洁，突出核心操作区域（查询、文档勾选、提交）。
  - 支持快速输入与权限查看。
  - 权限项以复选框或列表形式展示，直观易懂。
  - 所有操作均有明确的成功/失败反馈提示。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| 項目名 (Item)     | 種別 (Type)           | 必須 (Req) | MaxLength |     I/O      | 許容文字 (Allowed Chars) | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注                     |
| :---------------- | :-------------------- | :--------: | :-------: | :----------: | :----------------------- | :--------------: | :--------------: | :----------------: | :----------------------- |
| **UserID**        | TextField             |     Y      |    10     |    Input     | 半角英数字               |    左 (Left)     |    空 (Empty)    |   活性 (Enabled)   | 示例：a420064            |
| **User Info**     | Button                |     -      |     -     |    Action    | -                        |   中 (Center)    |        -         |   活性 (Enabled)   | 触发用户信息及权限加载   |
| **User Name**     | Label                 |     -      |    50     |    Output    | -                        |    左 (Left)     |        -         |   常显 (Visible)   | 显示来自 Saviynt 的姓名  |
| **Document List** | Checkbox Group / List |     -      |     -     | Input/Output | -                        |    左 (Left)     |    Unchecked     |   活性 (Enabled)   | 动态渲染所有可用文档类型 |
| **Update**        | Button                |     -      |     -     |    Action    | -                        |   中 (Center)    |        -         |   活性 (Enabled)   | 提交权限更新             |
| **Message**       | Label                 |     -      |    256    |    Output    | -                        |    左 (Left)     |    空 (Empty)    | 动态显示 (Dynamic) | 显示成功或错误信息       |

> **注**：
>
> - **Document List**：虽然 Prompt 中提到 TextArea，但为了“支持多选”和“勾选”，前端实现建议使用 Checkbox Group 或带有复选框的列表组件。若必须使用 TextArea，则需配合特定的交互逻辑（如双击选择），但 Checkbox 更符合 UX 要求。此处按 Checkbox Group 设计。
> - **初始化**：页面加载时即调用接口获取所有文档列表并展示（未选中状态），待输入 UserID 并查询后，再根据用户已有权限勾选对应项。

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

所有校验逻辑均在用户点击 **[User Info]** 或 **[Update]** 按钮时执行。

### 3.1 处理流程

1.  **开始**：页面加载或用户点击操作按钮。
2.  **初始化 (Page Mount)**：
    - 调用 `GET /api/UD18HDocUserDocAdministrationApi/UD20SelectHdocDocumentList` 获取所有可用文档类型。
    - 渲染文档列表，默认均为未选中状态。
3.  **前置处理**：获取 [UserID](file://e:\git20260511\NextInnovation\react-ud\src\Login_x\Login.tsx#L5-L5) 输入值，执行 `trim()`。
4.  **空值校验 (Frontend Check)**：
    - 若点击 **User Info** 或 **Update**：
      - 检查 [UserID](file://e:\git20260511\NextInnovation\react-ud\src\Login_x\Login.tsx#L5-L5) 是否为空。
      - 若为空，设置 `Message` = `请输入用户 ID`，**终止**流程。
5.  **API 调用 (Backend Check)**：
    - **User Info**:
      1. 调用 `GET /api/AuthenticationApi/Login` (注：Prompt 中以此接口获取用户信息，实际应为专门的 User Info 接口，此处依 Prompt 定义) 获取用户姓名。
      2. 同时/随后调用后端接口（如 `UD18SelectHdocUserDoc`）获取该用户已拥有的文档权限列表。
      3. 若 UserID 不存在于 `HDOC_FUNCTION_AUTH`，返回错误。
    - **Update**:
      1. 收集当前所有被勾选的文档 ID。
      2. 调用后端更新接口（Prompt 中列出了多个 Update/Delete/Create 接口，建议后端封装为一个统一的 Update 接口，或前端依次调用）。
      3. 后端校验 UserID 有效性。
6.  **结果处理**：
    - **User Info 成功**：
      - 显示 `User Name`。
      - 根据返回的权限列表，勾选 `Document List` 中对应的 Checkbox。
    - **User Info 失败 (404)**：显示 `We didn't recognize the userid you entered. Please try again.`，清空姓名和勾选状态。
    - **Update 成功**：提示“权限更新成功”。
    - **Update 失败**：提示具体错误（如“您没有权限执行此操作”或“用户不存在”）。

### 3.2 校验详细规格表

| No. |   检查时机   |     检查对象      |                检查条件                |                   错误消息 (Message Content)                    |           动作           |
| :-: | :----------: | :---------------: | :------------------------------------: | :-------------------------------------------------------------: | :----------------------: |
|  1  |  Click Any   |      UserID       | `Value == null` OR `Trim(Value) == ""` |                         `请输入用户 ID`                         |  显示 Message，焦点停留  |
|  2  | API Response |  User Existence   |     `Status == 404` (Info/Update)      | `We didn't recognize the userid you entered. Please try again.` | 显示 Message，清空权限区 |
|  3  | API Response | Update Permission |            `Status == 403`             |                     `您没有权限执行此操作`                      |       显示 Message       |
|  4  |  Page Mount  |   Doc List Load   |            `Status == 500`             |                 `Failed to load document list`                  |  显示错误，禁用后续操作  |

## 4. 接口定义 (API Specification)

### 4.1 Get Document List (获取文档列表 - 初始化)

- **功能**：获取系统中所有可分配的文档类型列表。
- **Method**: `GET`
- **Endpoint**: `/api/UD18HDocUserDocAdministrationApi/UD20SelectHdocDocumentList`

#### Response Success (200 OK)

```json
[
  "VIN-PLATE-VIN-Plate",
  "DIM-PLATE-DIM-Plate",
  "SASO-SASO-Plate",
  "COC_EU-COC (EU)",
  "XML003-XML003 - IVI",
  "CO2_CUST-CO2 Customer File",
  "CO2_NKS-COC_NKS",
  "CO2_NKS_PDF-COC_NKS (pdf)",
  "CO2_NKS_XML-COC_NKS (xml)",
  "XML_COC-COC_COC",
  "COC_DF-COC_Data File",
  "COC_EUP-COC_EUP",
  "DATEN-Data Confirmation",
  "DATE2-Data Confirmation (w/o weight calculation)",
  "ST15-State Registration",
  "ONREG-On-line registration file",
  "RDOC-Registration Document",
  "ADR-CHECK-ADR-Checklist (DEU only)",
  "KFB-KFZ-Brief (DEU only)"
]
```

### 4.2 Get User Info & Permissions (获取用户信息及现有权限)

_注：Prompt 中提及调用 `AuthenticationApi` 获取姓名，并隐含了获取权限的逻辑。此处合并描述或假设有一个专门的接口获取权限。_

- **功能**：验证用户存在性，获取姓名及当前文档权限。
- **Method**: `GET`
- **Endpoint**: `/api/AuthenticationApi/Login` (获取姓名) & `/api/UD18HDocUserDocAdministrationApi/UD18SelectHdocUserDoc` (获取权限)

#### Request (For Auth API)

```json
{
  "userId": "a420064"
}
```

#### Response Success (200 OK - Auth)

```json
{
  "name": "Jenna Wang",
  "email": "jenna.wang@volvo.com"
}
```

#### Response Success (200 OK - Permissions)

```json
{
  "documents": ["VIN-PLATE-VIN-Plate", "DIM-PLATE-DIM-Plate"]
}
```

### 4.3 Update User Doc Permissions (更新权限)

_注：Prompt 中列出了多个 Endpoint (`UD18SelectHdocUserDoc`, `UD18DeleteHdocUserDoc`, `UD18CreateHdocUserDoc`, `UD18SelectHdocFunctionAuth`)。最佳实践是提供一个统一的 Update 接口，由后端处理差异（新增/删除）。此处以统一接口为例。_

- **功能**：更新指定用户的文档权限集合。
- **Method**: `PUT` (或 POST)
- **Endpoint**: `/api/UD18HDocUserDocAdministrationApi/UD18UpdateUserDocPermissions` (建议统一命名)

#### Request Body

```json
{
  "userId": "a420064",
  "documents": ["VIN-PLATE-VIN-Plate", "DIM-PLATE-DIM-Plate", "SASO-SASO-Plate"]
}
```

#### Response Success (200 OK)

```json
{
  "success": true,
  "message": "权限更新成功"
}
```

#### Response Error (403 Forbidden)

```json
{
  "error": "Forbidden",
  "message": "您没有权限执行此操作"
}
```

#### Response Error (404 Not Found)

```json
{
  "error": "Not Found",
  "message": "We didn't recognize the userid you entered. Please try again."
}
```

## 5. 异常处理 (Exception Handling)

| 异常场景             | 处理方式           | Message 显示内容                                                |
| :------------------- | :----------------- | :-------------------------------------------------------------- |
| **网络断开/超时**    | 捕获 Network Error | `网络连接失败，请检查网络设置`                                  |
| **用户不存在**       | 捕获 API 404       | `We didn't recognize the userid you entered. Please try again.` |
| **权限不足**         | 捕获 API 403       | `您没有权限执行此操作`                                          |
| **数据库异常**       | 捕获 Server Error  | `系统繁忙，请稍后再试`                                          |
| **文档列表加载失败** | 捕获 API 500       | `Failed to load document list`                                  |

## 6. 实现注意事项 (Implementation Notes)

1.  **状态管理**：
    - 使用 React State 管理 `userId`, `userName`, `allDocuments` (列表), `userPermissions` (Set 或 Array), `message`, `isLoading`。
    - `allDocuments` 在 `useEffect` (Mount) 时加载。
    - `userPermissions` 在点击 `User Info` 成功后更新。
2.  **交互细节**：
    - **Loading 状态**：点击 `User Info` 或 `Update` 时，禁用相关按钮并显示 Loading 图标。
    - **Checkbox 绑定**：`Document List` 中的每个 Checkbox 的 `checked` 属性应绑定到 `userPermissions.includes(doc)`。
    - **Update 逻辑**：前端只需提交当前所有被选中的文档列表给后端，由后端决定哪些需要插入 `HDOC_USER_DOC` 表，哪些需要删除，简化前端逻辑。
    - **Message 样式**：成功消息绿色，错误消息红色。
3.  **安全性**：
    - API 请求必须通过 HTTPS 发送。
    - 后端需校验当前操作员是否有“用户文档权限管理”权限。
    - `UserId` 输入需进行 sanitization，防止注入攻击。
4.  **数据一致性**：
    - `User Name` 仅从 Saviynt (AuthenticationApi) 读取，不可在前端编辑。
    - 文档列表应从 `HDOC_DOCUMENT_LIST` 动态获取，避免硬编码。
