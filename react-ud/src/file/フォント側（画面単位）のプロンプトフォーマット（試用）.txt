请严格按照以下要求，生成一个规范、可直接运行的React登录页面完整代码，要求代码结构清晰、注释完整、无冗余，适配React 18版本，贴合前端开发规范，具体要求如下：

请参照该路径下的开发规约：E:\react\NextInnovation\react-ud\src\file\開発規約.txt 严格参照

 一、基础配置
1.  技术栈：React 18 + React Hooks（useState、useEffect）
2.  样式方案：【原生CSS】
3.  路由支持：【需要】（例：需要，使用react-router-dom实现登录跳转）
4.  请求工具：【axios】
5.  适配要求：PC端居中显示，支持响应式

 二、页面UI要求（以Login画面为例，可根据自己的画面进行修改）
1.  整体布局：
    
    1.2
    根据NextInnovation\react-ud\src\Generate\assets\GenerateDoc.png 图片生成一下画面代码，保持式样与图片一致，包括颜色，字体，位置一致 严格参照
    （由于每个画面会有不同状态下的画面表示，需要用不同的layout的图片生成代码）

 三、功能要求（核心，可修改接口参数）
 
    1.  根据NextInnovation\react-ud\src\Prompt\プロンプトUD04.txt 提示词描述，生成逻辑代码，严格参照

 四、代码规范要求
    1.  代码结构：拆分组件（请参照GenerateDoc.tsx + GenerateDoc.css），样式文件单独存放，不内嵌style，详细结构参照【1.1.项目结构】
    2.  注释要求：
            2.1.组件定义上方：添加JSDoc风格的注释，说明组件功能、Props参数类型和含义
            2.2.对API请求相关代码进行注释，根据内部设计中的描述，说明请求目的、参数和处理逻辑
            2.3.针对业务逻辑添加注释，解释内部设计中的业务规则如何实现
            2.4.内部设计的内容需要对应到代码相关处理中，注释 需与代码逻辑对应
    3.  错误处理：捕获请求异常、表单校验异常，无控制台报错
    4.  可复用性：核心函数（表单校验、密码显示隐藏）可单独抽取，方便后续复用
    5.  导出要求：默认导出GenerateDoc组件，可直接在App.tsx中引入使用

 五、禁止事项
    1.  禁止冗余代码、无效注释，禁止代码格式混乱