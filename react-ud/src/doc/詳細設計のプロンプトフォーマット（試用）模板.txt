请根据以下要求生成[模块名称]的详细设计文档：

1.基本信息
  模块名称：[填写模块名称]
  文档编号：[填写文档编号]
  版本号：v1.0
  作成日期：[YYYY-MM-DD]
  
2. 背景 (Background)
 请描述：
  本模块的核心功能和目标
  面向的用户群体
  用户体验方面的考虑
  安全性或特殊要求（如有）

3. 画面项目定义 (Screen Parameters)
请按照以下格式定义所有控件：

| No. | 項目名 (Item) | 種別 (Type) | 必須 (Req) | MaxLength | I/O | 許容文字 (Allowed Chars) | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注 |
|-----|--------------|-------------|-----------|-----------|-----|-------------------------|-----------------|-----------------|-------------------|------|
| 1   | [控件名称] | [TextField/Pull-downList/File Input/Button/Label等] | [Y/-] | [长度或-] | [Input/Output/Action] | [允许字符或-] | [左/中/右] | [空/默认值] | [活性/禁用/动态] | [备注说明] |
| 2   | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |


必须包含的控件类型：
- Input控件（如TextField、File Input、Pull-down List等）
- Output控件（如Label、Message等）
- Action控件（如Button等）

4. 功能描述 (Functional Description)
请描述每个主要功能：（PS：画面迁移等信息可以记述在这里）

    4.1 [功能名称1]
    功能说明：[简要描述功能]
    操作步骤：
      1. [步骤1]
      2. [步骤2]
      3. [步骤3]
      4. [结果反馈]
  
   4.2 [功能名称2]
    功能说明：[简要描述功能]
    操作步骤：
      1. [步骤1]
      2. [步骤2]
      3. [步骤3]
      4. [结果反馈]  

5. 业务逻辑与校验规则 (Business Logic & Checks)
请提供：
   5.1 处理流程
    按步骤描述每个主要功能的处理流程，格式如下：
      开始：[触发条件]
      前置处理：[数据获取和准备]
      空值校验等 (Frontend Check)：
       若[校验条件]：
        设置Message = [错误消息]
        终止流程
      5.1.2 API调用 (Backend Check)：
        调用AuthenticationApi
   5.2 结果处理：
       成功：[成功消息]
       失败：[失败消息]
   5.3 校验详细规格表
   | No. | 检查时机 | 检查对象 | 检查条件 | 错误消息 (Message Content) | 动作 |
   |-----|---------|---------|---------|--------------------------|------|
   | [序号] | [触发事件] | [校验字段] | [校验规则] | [错误提示] | [系统行为] |


6. 接口定义 (API Specification)
请为每个API提供(TODO：API的具体内容参照API提示词)：

   [API名称]
     功能：[API功能描述]
     Method：[GET/POST/PUT/DELETE]
     Endpoint：[API路径]

     Request [请求格式]
     [请求示例]

     Response Success ([状态码])
     [成功响应示例]

     Response Error ([状态码])
     [错误响应示例]

7. 异常处理 (Exception Handling)（TODO：共同化可能）
| 异常场景 | 处理方式 | Message显示内容 |
|---------|---------|----------------|
| [异常类型] | [处理方法] | [错误消息] |

8.语言要求
  文档使用中文编写
  所有错误消息和成功消息使用中文
  技术术语可保留英文
  保持与参考文档（内部設計模板.md）相同的格式结构

9.输出要求
  生成完整的Markdown格式文档
  文件名格式：[模块功能名]_詳細設計.md
  保存到：当前文件夹下的work目录
