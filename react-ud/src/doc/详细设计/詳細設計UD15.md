# VIN Plate页面 (VinPlatePage) 详细设计说明书

| 文档编号     | DES-VinPlatePage-001        | 版本号     | v1.0       |
| :----------- | :-------------------------- | :--------- | :--------- |
| **模块名称** | VIN Plate (Vin Plate)       | **作成日** | 2026-05-20 |
| **作成者**   | AI Assistant                | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块用于查看和管理车辆底盘相关信息，主要功能包括输入底盘号查询VIN Plate详细信息、更新底盘状态为"0"（新規追加）、更新底盘状态为"1"（xml doc 作成済み）、将底盘状态更新为"0"并设置TYPE为"1"（basic）、将底盘状态更新为"0"并设置TYPE为"2"（ADVANCED with weights）。

- **目标**：提供VIN Plate底盘状态的管理功能。
- **面向用户**：需要管理和维护车辆底盘信息的授权用户，如数据管理员、质量管理人员等。
- **用户体验**：简洁的操作界面，五个功能按钮清晰明了、明确的错误提示信息、快速的状态更新操作。
- **安全性**：底盘号必须存在于系统中才能执行操作、防止非法字符输入、支持HTTPS安全传输。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| No | 項目名 | 種別 | 必須 | MaxLength | データ元_テーブル | データ元_項目 | I/O | 許容文字 | 文字配置 | 初期値 | 表示制御 | 備考 |
| :-: | :--- | :---: | :-: | :-: | :--- | :---: | :-: | :-: | :-: | :-: | :-: | :--- |
| 1 | **Chassis number** | textfield | - | 15 | - | - | Input | - | 左 (Left) | - | 活性 (Enabled) | 底盘号输入框 |
| 2 | **View Info** | button | - | - | - | - | - | - | 中 (Center) | - | 活性 (Enabled) | 查看VIN Plate详细信息 |
| 3 | **Set Regenerate** | button | - | - | - | - | - | - | 中 (Center) | - | 活性 (Enabled) | 将底盘状态更新为"0"（新規追加） |
| 4 | **Set OK** | button | - | - | - | - | - | - | 中 (Center) | - | 活性 (Enabled) | 将底盘状态更新为"1"（xml doc 作成済み） |
| 5 | **Change to Basic Info** | button | - | - | - | - | - | - | 中 (Center) | - | 活性 (Enabled) | 将状态更新为"0"，TYPE更新为"1"（basic） |
| 6 | **Change to Advanced Info** | button | - | - | - | - | - | - | 中 (Center) | - | 活性 (Enabled) | 将状态更新为"0"，TYPE更新为"2"（ADVANCED with weights） |
| 7 | **Chassis number** | label | - | - | 画面設定内容 | Chassis number | Output | - | 左 (Left) | - | - | 显示当前输入的底盘号 |
| 8 | **Plate type** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | TYPE | Output | - | 左 (Left) | - | - | VIN Plate的类型（1=basic / 2=ADVANCED with weights） |
| 9 | **Status** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | STATUS | Output | - | 左 (Left) | - | - | VIN Plate的状态（0=新規追加 / 1=xml doc 作成済み） |
| 10 | **Error Message** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | MSG | Output | - | 左 (Left) | - | - | 消息文本 |
| 11 | **Def.** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | REGISTER_DATETIME | Output | - | 左 (Left) | - | - | 注册日期时间 |
| 12 | **Data ready** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | DOC_READY | Output | - | 左 (Left) | - | - | 文档就绪标志 |
| 13 | **Sent to CAB factory** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | DOC_SENT | Output | - | 左 (Left) | - | - | 文档发送标志 |
| 14 | **Print items** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | XML_DOC | Output | - | 左 (Left) | - | - | XML_DOC中的PrintItemName值 |
| 15 | **VP Data** | label | - | - | HDOC_SEND_DATA_VIN_PLATE | XML_DOC | Output | - | 左 (Left) | - | - | XML_DOC中各Variant名与Value |

> **注**：
> - Status字段含义："0"=新規追加，"1"=xml doc 作成済み
> - Plate type字段含义："1"=basic，"2"=ADVANCED (with weights)
> - Print items和VP Data均从XML_DOC字段解析获取

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

### 3.1 处理流程

所有按钮操作的共同前置处理：获取Chassis number字段值，若为空则显示"Chassis number is required."并**终止**流程，不调用API。

#### 3.1.1 View Info处理流程

1. **开始**：用户点击View Info按钮。
2. **前置校验**：Chassis number必填检查。
3. 调用`UD15SelecthdocsenddatavinplateApi`的`UD15ViewInfo`方法，operation为"viewInfo"。
4. **结果处理**：
   - **成功**：将API返回的TYPE(Plate type)、STATUS(Status)、MSG(Error Message)、REGISTER_DATETIME(Def.)、DOC_READY(Data ready)、DOC_SENT(Sent to CAB factory)、XML_DOC(Print items / VP Data)分别显示到对应的Label控件。
   - **失败（底盘号不存在）**：显示"Chassis number XXX not found."，清空各显示字段。

#### 3.1.2 Set Regenerate处理流程

1. **开始**：用户点击Set Regenerate按钮。
2. **前置校验**：Chassis number必填检查。
3. 调用`UD15SelecthdocsenddatavinplateApi`的`UD15SetRegenerate`方法，operation为"setRegenerate"。
4. 将HDOC_SEND_DATA_VIN_PLATE表中对应底盘号的Status字段更新为"0"。
5. **结果处理**：
   - **成功**：显示"Status updated successfully."。
   - **失败**：显示"Chassis number XXX not found."。

#### 3.1.3 Set OK处理流程

1. **开始**：用户点击Set OK按钮。
2. **前置校验**：Chassis number必填检查。
3. 调用`UD15SelecthdocsenddatavinplateApi`的`UD15SetOK`方法，operation为"setOK"。
4. 将Status字段更新为"1"。
5. **结果处理**：同Set Regenerate。

#### 3.1.4 Change to Basic Info处理流程

1. **开始**：用户点击Change to Basic Info按钮。
2. **前置校验**：Chassis number必填检查。
3. 调用`UD15SelecthdocsenddatavinplateApi`的`UD15ChangetoBasicInfo`方法，operation为"changeToBasicInfo"。
4. 将Status更新为"0"，Type更新为"1"。
5. **结果处理**：
   - **成功**：显示"Status and Type updated successfully."。
   - **失败**：显示"Chassis number XXX not found."。

#### 3.1.5 Change to Advanced Info处理流程

1. **开始**：用户点击Change to Advanced Info按钮。
2. **前置校验**：Chassis number必填检查。
3. 调用`UD15SelecthdocsenddatavinplateApi`的`UD15ChangetoAdvancedInfo`方法，operation为"changeToAdvancedInfo"。
4. 将Status更新为"0"，Type更新为"2"。
5. **结果处理**：同Change to Basic Info。

### 3.2 校验详细规格表

| No. | 检查时机 | 检查对象 | 检查条件 | 错误消息 (Message Content) | 动作 |
| :-: | :---: | :---: | :--- | :--- | :---: |
| 1 | 点击任意按钮时 | Chassis number | 值为空字符串或未填写 | `Chassis number is required.` | 显示错误消息，终止流程，不调用API |
| 2 | API调用后 | 底盘号存在性 | API返回底盘号不存在 | `Chassis number XXX not found.` | 显示Warning级别消息，保持当前页面 |

## 4. 接口定义 (API Specification)

### UD15SelecthdocsenddatavinplateApi

统一API名称空间，包含以下方法：**UD15ViewInfo**（查看VIN Plate信息）、**UD15SetRegenerate**（设置再生成）、**UD15SetOK**（设置为OK）、**UD15ChangetoBasicInfo**（变更为基本信息）、**UD15ChangetoAdvancedInfo**（变更为高级信息）。

---

### 4.1 UD15ViewInfo - 查看VIN Plate信息

**功能**：根据底盘号查询VIN Plate详细信息

- **Method**：`POST`
- **Endpoint**：`/api/ud15/UD15SelecthdocsenddatavinplateApi`

#### Request

```json
{
  "chassisNumber": "string (Max 15, Required)",
  "operation": "viewInfo"
}
```

#### Response Success (200)

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "chassisNumber": "string",
    "type": "string",
    "status": "string",
    "msg": "string",
    "registerDatetime": "2024-01-01T00:00:00",
    "docReady": "string",
    "docSent": "string",
    "xmlDoc": "string"
  }
}
```

#### Response Error (404)

```json
{
  "code": 404,
  "message": "Chassis number XXX not found.",
  "data": null
}
```

#### Response Error (500)

```json
{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

### 4.2 UD15SetRegenerate - 设置再生成

**功能**：将底盘状态更新为"0"（新規追加）

- **Method**：`POST`
- **Endpoint**：`/api/ud15/UD15SelecthdocsenddatavinplateApi`

#### Request

```json
{
  "chassisNumber": "string (Max 15, Required)",
  "operation": "setRegenerate"
}
```

#### Response Success (200)

```json
{
  "code": 200,
  "message": "Status updated to 0 (New Addition)",
  "data": null
}
```

#### Response Error (404)

```json
{
  "code": 404,
  "message": "Chassis number XXX not found.",
  "data": null
}
```

#### Response Error (500)

```json
{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

### 4.3 UD15SetOK - 设置为OK

**功能**：将底盘状态更新为"1"（xml doc 作成済み）

- **Method**：`POST`
- **Endpoint**：`/api/ud15/UD15SelecthdocsenddatavinplateApi`

#### Request

```json
{
  "chassisNumber": "string (Max 15, Required)",
  "operation": "setOK"
}
```

#### Response Success (200)

```json
{
  "code": 200,
  "message": "Status updated to 1 (XML Doc Created)",
  "data": null
}
```

#### Response Error (404)

```json
{
  "code": 404,
  "message": "Chassis number XXX not found.",
  "data": null
}
```

#### Response Error (500)

```json
{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

### 4.4 UD15ChangetoBasicInfo - 变更为基本信息

**功能**：将底盘状态更新为"0"，TYPE更新为"1"（basic）

- **Method**：`POST`
- **Endpoint**：`/api/ud15/UD15SelecthdocsenddatavinplateApi`

#### Request

```json
{
  "chassisNumber": "string (Max 15, Required)",
  "operation": "changeToBasicInfo"
}
```

#### Response Success (200)

```json
{
  "code": 200,
  "message": "Status updated to 0 and Type updated to 1 (Basic)",
  "data": null
}
```

#### Response Error (404)

```json
{
  "code": 404,
  "message": "Chassis number XXX not found.",
  "data": null
}
```

#### Response Error (500)

```json
{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

### 4.5 UD15ChangetoAdvancedInfo - 变更为高级信息

**功能**：将底盘状态更新为"0"，TYPE更新为"2"（ADVANCED with weights）

- **Method**：`POST`
- **Endpoint**：`/api/ud15/UD15SelecthdocsenddatavinplateApi`

#### Request

```json
{
  "chassisNumber": "string (Max 15, Required)",
  "operation": "changeToAdvancedInfo"
}
```

#### Response Success (200)

```json
{
  "code": 200,
  "message": "Status updated to 0 and Type updated to 2 (ADVANCED with weights)",
  "data": null
}
```

#### Response Error (404)

```json
{
  "code": 404,
  "message": "Chassis number XXX not found.",
  "data": null
}
```

#### Response Error (500)

```json
{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

## 5. 异常处理 (Exception Handling)

| 异常场景 | 处理方式 | Message 显示内容 |
| :--- | :--- | :--- |
| **网络连接失败** | 捕获网络异常，显示通用错误消息 | `System error. Please contact administrator.` |
| **API超时** | 设置超时时间（建议10秒），超时后显示错误 | `System error. Please contact administrator.` |
| **服务器内部错误** | API返回500，显示系统错误消息 | `System error. Please contact administrator.` |
| **JSON解析失败** | 捕获解析异常，显示系统错误 | `System error. Please contact administrator.` |

## 6. 实现注意事项 (Implementation Notes)

1. **状态管理**：
   - 使用React State管理Chassis number输入值和错误消息。
   - 所有按钮在API调用期间应设为`disabled`，防止重复提交。
2. **操作类型**：所有按钮操作都调用同一个API端点，通过`operation`参数区分不同功能。
3. **底盘号验证**：所有按钮操作都需要先验证底盘号是否存在，不存在时显示Warning级别的错误消息。
4. **UI细节**：
   - 页面标题显示为"EDB Engineering Database - Vin Plate"。
   - 五个功能按钮清晰排列，每个按钮对应一种状态操作。
   - 操作成功后应显示相应的成功消息。

---
