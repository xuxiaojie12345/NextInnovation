# HDocTemplateCheck详细设计说明书

| 文档编号     | DES-HDocTemplateCheckPage-001 | 版本号     | v1.0       |
| :----------- | :---------------------------- | :--------- | :--------- |
| **模块名称** | HDocTemplateCheckPage         | **作成日** | 2026-05-20 |
| **作成者**   | Qoder                         | **状态**   | 正式稿     |

## 1. 背景 (Background)

本模块用于检查HDoc模板文件的格式和内容，主要功能包括：支持选择并上传rtf格式的模板文件、读取文件内容，解析$符号之间的变量、统计变量数量、验证文件格式是否正确、提供检查后的模板下载链接、仅适用于rtf文件检查。

- **目标**：向需要验证认证系统模板文件格式和内容的授权用户提供模板检查功能。
- **安全性**：需要用户登录认证后才能访问；文件上传需要进行类型验证（仅允许rtf文件）；支持HTTPS安全传输协议。
- **用户体验**：清晰的文件选择界面；明确的检查结果反馈；便捷的下载功能；详细的错误提示信息。

## 2. 画面参数定义 (Screen Parameters)

### 2.1 控件属性表

根据需求，各输入输出项的详细定义如下：

| 項目名 (Item)                 | 種別 (Type) | 必須 (Req) | MaxLength |  I/O   | 許容文字 (Allowed Chars) | 文字配置 (Align) | 初期値 (Default) | 表示制御 (Control) | 备注                                        |
| :---------------------------- | :---------- | :--------: | :-------: | :----: | :----------------------- | :--------------: | :--------------: | :----------------: | :------------------------------------------ |
| **Template File**             | File Input  |     -      |     -     | Input  | -                        |    左 (Left)     |    空 (Empty)    |   活性 (Enabled)   | 文件选择输入框，用于选择要检查的rtf模板文件 |
| **Check**                     | Button      |     -      |     -     | Action | -                        |    左 (Left)     |        -         |   活性 (Enabled)   | 执行模板文件检查操作                        |
| **Download checked template** | Link        |     -      |     -     | Output | -                        |    左 (Left)     |        -         |   活性 (Enabled)   | 检查成功后显示，点击下载检查后的模板文件    |

> **注**：
>
> - Input控件：File Input为输入控件，用于接收用户上传的文件
> - Action控件：Check按钮用于触发文件检查操作
> - Output控件：Download checked template链接为输出控件，检查成功后显示

## 3. 业务逻辑与校验规则 (Business Logic & Checks)

所有校验逻辑均在页面加载时或用户执行操作时执行。

### 3.1 处理流程

#### 3.1.1 页面初始化流程

1.  **开始**：页面加载。
2.  **处理步骤**：

- 清空文件选择框。
- 清空错误消息和成功消息区域。
- 隐藏Download checked template链接。
- 页面处于等待用户选择文件的状态。

3.  **结果处理**：

- **成功**：页面初始化完成，等待用户操作。

### 3.2 校验详细规格表

| No. |    检查时机     | 检查对象 |             检查条件              |       错误消息 (Message Content)        |             动作             |
| :-: | :-------------: | :------: | :-------------------------------: | :-------------------------------------: | :--------------------------: |
|  1  | 点击Check按钮时 | 文件选择 |          未选择任何文件           |     `ERROR: Unable to access file!`     |    显示错误消息，终止流程    |
|  2  | 文件内容解析后  | 变量数量 | 文件中没有找到任何$...$格式的变量 | `ERROR: The file content is incorrect!` | 显示错误消息，不显示下载链接 |
|  3  |  文件读取失败   | 文件访问 |         无法读取文件内容          |     `ERROR: Unable to access file!`     |    显示错误消息，终止流程    |

## 4. 接口定义 (API Specification)

本模块不调用后端API，所有处理在前端完成。

## 5. 异常处理 (Exception Handling)

| 异常场景                   | 处理方式                                | Message 显示内容                              |
| :------------------------- | :-------------------------------------- | :-------------------------------------------- |
| **未选择文件点击Check**    | 显示文件选择提示消息                    | `ERROR: Unable to access file!`               |
| **文件读取失败**           | 捕获读取异常，显示错误消息              | `ERROR: Unable to access file!`               |
| **文件中没有变量**         | 显示内容错误消息                        | `ERROR: The file content is incorrect!`       |
| **文件类型不正确**         | 可选：显示文件类型提示                  | `Please select an RTF file.`                  |
| **未预期的JavaScript错误** | 使用try-catch包裹关键代码，记录错误日志 | `System error. Please contact administrator.` |
| **用户未登录**             | 检查会话状态，跳转到登录页面            | `Please login first`                          |

## 6. 实现注意事项 (Implementation Notes)

1.  **状态管理**：
    - 使用 React State 管理 templateFile, variables, variableCount, errorMessage, successMessage, downloadLink, `isLoading`。
    - Download checked template 链接默认隐藏，检查成功后显示。
2.  **安全性**：
    - 需要用户登录认证后才能访问。
    - 文件上传需要进行类型验证（仅允许rtf文件）。
3.  **UI 细节**：
    - Check 按钮在 `isLoading` 期间应设为 `disabled`，防止重复提交。
    - Message 文字颜色建议使用红色 (`#ff4d4f`) 以起到警示作用。
    - 成功消息使用绿色 (`#52c41a`) 显示。
    - 文件读取使用FileReader API，变量提取使用正则表达式匹配$...$格式。

---
