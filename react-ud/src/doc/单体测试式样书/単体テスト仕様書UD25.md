# EDB User View 模块 (UD25) 测试规格书

| 文档编号     | TEST-UD25-001  | 版本号       | v1.0       |
| :----------- | :------------- | :----------- | :--------- |
| **模块名称** | EDB User View  | **创建日期** | 2026-06-30 |
| **创建者**   | GitHub Copilot | **状态**     | 正式稿     |

---

## 测试用例明细

### 画面初始化

| No  | 测试对象                  | 操作步骤                                                                  | 设定值       | 预期结果                                                                                                                                                                                          | 确认结果 | 确认时间 |
| :-: | :------------------------ | :------------------------------------------------------------------------ | :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :------- | :------- |
|  1  | 画面初期显示-基本元素     | 1. 从 UD09/UD11/UD20 页面跳转到 EDB User View 页面<br>2. 确认画面整体显示 | 无           | 1. 显示用户信息字段：Userid、Responsible、User Position、E-mail<br>2. 显示 Clear 按钮<br>3. 显示 Back 按钮<br>4. 所有字段为只读显示（TextField Output）                                           |          |          |
|  2  | 画面初期显示-用户信息加载 | 1. 页面加载时<br>2. 确认 API 调用及数据填充                               | 无           | 1. 接收前画面传递的 userid 参数<br>2. 自动调用 POST `/api/login`<br>3. 请求体包含 `{"userID": "xxx"}`<br>4. API 返回个人信息<br>5. 各字段填充对应数据：Userid、Responsible、User Position、E-mail |          |          |
|  3  | 画面初期显示-加载中状态   | 1. 模拟 API 响应延迟（2秒）<br>2. 访问页面<br>3. 确认加载中的画面显示     | 无           | 1. isLoading 为 true 时显示加载指示器<br>2. Clear 和 Back 按钮处于禁用状态（disabled）<br>3. 字段内容为空                                                                                         |          |          |
|  4  | 画面初期显示-API 调用失败 | 1. 模拟 API 返回 500 错误<br>2. 访问页面<br>3. 确认结果                   | API 返回 500 | 1. 所有字段显示为空<br>2. 不显示错误消息<br>3. Clear 和 Back 按钮恢复正常状态                                                                                                                     |          |          |
|  5  | 画面初期显示-用户未登录   | 1. 清除登录状态<br>2. 访问页面<br>3. 确认结果                             | 无           | 1. 检测到未登录或会话过期<br>2. 显示错误消息："用户未登录或会话已过期，请重新登录"<br>3. 跳转到登录页面                                                                                           |          |          |
|  6  | 画面初期显示-网络异常     | 1. 模拟网络断开<br>2. 访问页面<br>3. 确认结果                             | 无           | 1. 所有字段显示为空<br>2. 不显示错误消息<br>3. Clear 和 Back 按钮恢复正常状态                                                                                                                     |          |          |

### 用户信息显示

| No  | 测试对象                    | 操作步骤                                                   | 设定值 | 预期结果                                                                                                                                                     | 确认结果 | 确认时间 |
| :-: | :-------------------------- | :--------------------------------------------------------- | :----- | :----------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
|  7  | 用户信息-Userid 显示        | 1. API 返回数据后<br>2. 确认 Userid 字段显示               | 无     | 1. Userid 字段显示当前登录用户的 ID<br>2. 数据来源：Saviynt 系统的 Userid<br>3. 左对齐显示<br>4. 只读状态                                                    |          |          |
|  8  | 用户信息-Responsible 显示   | 1. API 返回数据后<br>2. 确认 Responsible 字段显示          | 无     | 1. Responsible 字段显示当前登录用户的负责人<br>2. 数据来源：Saviynt 系统的 Responsible<br>3. 左对齐显示<br>4. 只读状态                                       |          |          |
|  9  | 用户信息-User Position 显示 | 1. API 返回数据后<br>2. 确认 User Position 字段显示        | 无     | 1. User Position 字段显示当前登录用户的职位<br>2. 数据来源：Saviynt 系统的 User Position<br>3. 左对齐显示<br>4. 只读状态                                     |          |          |
| 10  | 用户信息-E-mail 显示        | 1. API 返回数据后<br>2. 确认 E-mail 字段显示               | 无     | 1. E-mail 字段显示当前登录用户的邮箱地址<br>2. 数据来源：Saviynt 系统的 E-mail<br>3. 左对齐显示<br>4. 只读状态                                               |          |          |
| 11  | 用户信息-字段只读状态       | 1. API 返回数据后<br>2. 尝试编辑各字段                     | 无     | 1. Userid 字段不可编辑<br>2. Responsible 字段不可编辑<br>3. User Position 字段不可编辑<br>4. E-mail 字段不可编辑<br>5. 所有字段为纯展示（readonly/disabled） |          |          |
| 12  | 用户信息-API 返回空字段     | 1. 模拟 API 返回某个字段为空<br>2. 访问页面<br>3. 确认显示 | 无     | 1. 对应字段显示为空<br>2. 其他字段正常显示<br>3. 页面不报错                                                                                                  |          |          |

### Clear 按钮

| No  | 测试对象                         | 操作步骤                                                                            | 设定值 | 预期结果                                                                                                                            | 确认结果 | 确认时间 |
| :-: | :------------------------------- | :---------------------------------------------------------------------------------- | :----- | :---------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 13  | Clear 按钮-清空所有字段          | 1. 用户信息加载完成后<br>2. 点击 Clear 按钮<br>3. 确认各字段状态                    | 无     | 1. Userid 字段被清空<br>2. Responsible 字段被清空<br>3. User Position 字段被清空<br>4. E-mail 字段被清空<br>5. 所有字段保持只读状态 |          |          |
| 14  | Clear 按钮-连续点击              | 1. 用户信息加载完成后<br>2. 连续多次点击 Clear 按钮<br>3. 确认各字段状态            | 无     | 1. 第一次点击后所有字段已清空<br>2. 后续点击无变化<br>3. 页面不报错                                                                 |          |          |
| 15  | Clear 按钮-加载中禁用            | 1. 页面加载中（isLoading=true）<br>2. 确认 Clear 按钮状态<br>3. 尝试点击 Clear 按钮 | 无     | 1. Clear 按钮处于禁用状态（disabled）<br>2. 点击无效<br>3. API 响应完成后按钮恢复正常                                               |          |          |
| 16  | Clear 按钮-清空后不发送 API 请求 | 1. 点击 Clear 按钮<br>2. 确认是否有网络请求                                         | 无     | 1. Clear 按钮只清空前端字段显示<br>2. 不发送任何 API 请求<br>3. 不进行数据保存操作                                                  |          |          |

### Back 按钮

| No  | 测试对象             | 操作步骤                                                                          | 设定值 | 预期结果                                                                             | 确认结果 | 确认时间 |
| :-: | :------------------- | :-------------------------------------------------------------------------------- | :----- | :----------------------------------------------------------------------------------- | :------- | :------- |
| 17  | Back 按钮-返回前画面 | 1. 页面加载完成后<br>2. 点击 Back 按钮<br>3. 确认页面跳转                         | 无     | 1. 页面跳转到前一个画面（UD09/UD11/UD20）<br>2. 跳转正确                             |          |          |
| 18  | Back 按钮-加载中禁用 | 1. 页面加载中（isLoading=true）<br>2. 确认 Back 按钮状态<br>3. 尝试点击 Back 按钮 | 无     | 1. Back 按钮处于禁用状态（disabled）<br>2. 点击无效<br>3. API 响应完成后按钮恢复正常 |          |          |
| 19  | Back 按钮-返回失败   | 1. 模拟页面跳转失败<br>2. 点击 Back 按钮<br>3. 确认结果                           | 无     | 1. 显示错误消息："页面跳转失败，请刷新页面重试"<br>2. 保持在当前页面                 |          |          |

### 异常处理

| No  | 测试对象              | 操作步骤                                           | 设定值       | 预期结果                                                                            | 确认结果 | 确认时间 |
| :-: | :-------------------- | :------------------------------------------------- | :----------- | :---------------------------------------------------------------------------------- | :------- | :------- |
| 20  | 异常处理-API 超时     | 1. 模拟 API 响应超时<br>2. 访问页面<br>3. 确认结果 | 无           | 1. 超时后所有字段显示为空<br>2. 不显示错误消息<br>3. Clear 和 Back 按钮恢复正常状态 |          |          |
| 21  | 异常处理-401 未授权   | 1. 模拟 API 返回 401<br>2. 访问页面<br>3. 确认结果 | API 返回 401 | 1. 显示错误消息："用户未登录或会话已过期，请重新登录"<br>2. 跳转到登录页面          |          |          |
| 22  | 异常处理-错误消息样式 | 1. 触发用户未登录错误<br>2. 确认错误消息样式       | 无           | 1. 错误消息显示红色（#ff4d4f）<br>2. 默认隐藏（内容为空时不占位或高度为 0）         |          |          |

### 安全性

| No  | 测试对象            | 操作步骤                                                        | 设定值 | 预期结果                                                                                                                     | 确认结果 | 确认时间 |
| :-: | :------------------ | :-------------------------------------------------------------- | :----- | :--------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 23  | 安全性-API 请求协议 | 1. 打开浏览器开发者工具<br>2. 执行页面初始化<br>3. 检查网络请求 | 无     | 1. API 请求通过 HTTPS 发送<br>2. 请求中包含 userID 参数<br>3. 请求中不泄露其他敏感信息                                       |          |          |
| 24  | 安全性-只读数据     | 1. 确认页面数据类型                                             | 无     | 1. 所有用户信息为只读展示<br>2. 不允许用户修改<br>3. 无输入框或可编辑控件<br>4. 仅显示当前登录用户的信息，不显示其他用户数据 |          |          |

---
