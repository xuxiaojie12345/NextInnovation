# AD Change 模块 (UD16) 测试规格书

| 文档编号     | TEST-UD16-001  | 版本号       | v1.0       |
| :----------- | :------------- | :----------- | :--------- |
| **模块名称** | AD Change      | **创建日期** | 2026-06-30 |
| **创建者**   | GitHub Copilot | **状态**     | 正式稿     |

---

## 测试用例明细

### 画面初始化

| No  | 测试对象                    | 操作步骤                                      | 设定值 | 预期结果                                                                                                                                          | 确认结果 | 确认时间 |
| :-: | :-------------------------- | :-------------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------ | :------- | :------- |
|  1  | 画面初期显示-基本元素       | 1. 访问 AD Change 页面<br>2. 确认画面整体显示 | 无     | 1. 显示 Serie-Chnr 输入框（最大长度 15）<br>2. 显示 Desc 输入框（最大长度 4000）<br>3. 显示按钮：ADD、DELETE、CHECK<br>4. 所有按钮可用（Enabled） |          |          |
|  2  | 画面初期显示-输入框初始状态 | 1. 访问页面<br>2. 确认各输入框状态            | 无     | 1. Serie-Chnr 输入框为空<br>2. Desc 输入框为空<br>3. 无错误消息和成功消息显示                                                                     |          |          |

### ADD 操作

| No  | 测试对象                        | 操作步骤                                                                                 | 设定值                                        | 预期结果                                                                                                                                   | 确认结果 | 确认时间 |
| :-: | :------------------------------ | :--------------------------------------------------------------------------------------- | :-------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
|  3  | ADD-Serie-Chnr 为空             | 1. Serie-Chnr 为空或仅包含空格<br>2. 点击 ADD 按钮<br>3. 确认结果                        | Serie-Chnr: ``                                | 1. 显示错误消息："Serie-Chnr不能为空"<br>2. 终止流程，不调用 API<br>3. ADD 按钮恢复可用状态                                                |          |          |
|  4  | ADD-Serie-Chnr 长度超过 15 字符 | 1. Serie-Chnr 输入超过 15 字符<br>2. 点击 ADD 按钮<br>3. 确认结果                        | Serie-Chnr: 16 字符以上                       | 1. 显示错误消息："Serie-Chnr长度不能超过15字符"<br>2. 终止流程，不调用 API                                                                 |          |          |
|  5  | ADD-Desc 长度超过 4000 字符     | 1. Desc 输入超过 4000 字符<br>2. 点击 ADD 按钮<br>3. 确认结果                            | Desc: 4001 字符以上                           | 1. 显示错误消息："Desc长度不能超过4000字符"<br>2. 终止流程，不调用 API                                                                     |          |          |
|  6  | ADD-成功                        | 1. 输入有效 Serie-Chnr 和 Desc<br>2. 点击 ADD 按钮<br>3. API 返回成功<br>4. 确认结果     | Serie-Chnr: `JPCT-G28321`<br>Desc: `描述信息` | 1. 调用 API（POST `/api/ud16/add`）<br>2. API 返回 code=200<br>3. 显示成功消息："AD Change记录添加成功"（绿色）<br>4. 输入框被清空（可选） |          |          |
|  7  | ADD-记录已存在且未激活          | 1. 输入已存在且未激活的 Serie-Chnr<br>2. 点击 ADD 按钮<br>3. API 返回 400<br>4. 确认结果 | Serie-Chnr 已存在且未激活                     | 1. 显示警告消息："AFTER DEF CHANGE IS NOT ACTIVATED"<br>2. 类型为 Warning 级别                                                             |          |          |
|  8  | ADD-失败（API 返回 500）        | 1. 输入有效值<br>2. 点击 ADD 按钮<br>3. API 返回 500<br>4. 确认结果                      | API 返回 500                                  | 1. 显示错误消息："系统错误，请稍后重试"<br>2. ADD 按钮恢复可用状态                                                                         |          |          |
|  9  | ADD-加载中按钮禁用              | 1. 输入有效值<br>2. 点击 ADD 按钮<br>3. 在 API 响应前确认按钮状态                        | 有效值                                        | 1. ADD 按钮在加载期间禁用（disabled）<br>2. 防止重复提交                                                                                   |          |          |

### DELETE 操作

| No  | 测试对象                         | 操作步骤                                                                                                     | 设定值                    | 预期结果                                                                                                                                                                          | 确认结果 | 确认时间 |
| :-: | :------------------------------- | :----------------------------------------------------------------------------------------------------------- | :------------------------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 10  | DELETE-Serie-Chnr 为空           | 1. Serie-Chnr 为空<br>2. 点击 DELETE 按钮<br>3. 确认结果                                                     | Serie-Chnr: ``            | 1. 显示错误消息："Serie-Chnr不能为空"<br>2. 终止流程，不弹出确认对话框<br>3. 不调用 API                                                                                           |          |          |
| 11  | DELETE-确认对话框-点击取消       | 1. 输入有效 Serie-Chnr<br>2. 点击 DELETE 按钮<br>3. 确认对话框出现<br>4. 点击取消<br>5. 确认结果             | Serie-Chnr: `JPCT-G28321` | 1. 弹出确认对话框，显示 "Do you really want to delete this AD Change record?"<br>2. 点击取消后流程终止<br>3. 不调用 API<br>4. DELETE 按钮恢复可用状态                             |          |          |
| 12  | DELETE-确认对话框-点击确定并成功 | 1. 输入有效 Serie-Chnr<br>2. 点击 DELETE 按钮<br>3. 确认对话框点击确定<br>4. API 返回成功<br>5. 确认结果     | Serie-Chnr: `JPCT-G28321` | 1. 弹出确认对话框<br>2. 点击确定后调用 API（DELETE `/api/ud16/delete`）<br>3. API 返回 code=200<br>4. 显示成功消息："AD Change记录删除成功"（绿色）<br>5. Serie-Chnr 输入框被清空 |          |          |
| 13  | DELETE-记录不存在                | 1. 输入不存在的 Serie-Chnr<br>2. 点击 DELETE 按钮<br>3. 确认对话框点击确定<br>4. API 返回 400<br>5. 确认结果 | Serie-Chnr 不存在         | 1. 显示错误消息："记录不存在，无法删除"<br>2. DELETE 按钮恢复可用状态                                                                                                             |          |          |
| 14  | DELETE-失败（API 返回 500）      | 1. 输入有效值<br>2. 点击 DELETE 按钮<br>3. 确认对话框点击确定<br>4. API 返回 500<br>5. 确认结果              | API 返回 500              | 1. 显示错误消息："系统错误，请稍后重试"<br>2. DELETE 按钮恢复可用状态                                                                                                             |          |          |
| 15  | DELETE-加载中按钮禁用            | 1. 输入有效值<br>2. 点击 DELETE 按钮<br>3. 确认对话框点击确定<br>4. 在 API 响应前确认按钮状态                | 有效值                    | 1. DELETE 按钮在加载期间禁用（disabled）<br>2. 防止重复提交                                                                                                                       |          |          |

### CHECK 操作

| No  | 测试对象                   | 操作步骤                                                                                  | 设定值                  | 预期结果                                                                                      | 确认结果 | 确认时间 |
| :-: | :------------------------- | :---------------------------------------------------------------------------------------- | :---------------------- | :-------------------------------------------------------------------------------------------- | :------- | :------- |
| 16  | CHECK-Serie-Chnr 为空      | 1. Serie-Chnr 为空<br>2. 点击 CHECK 按钮<br>3. 确认结果                                   | Serie-Chnr: ``          | 1. 显示错误消息："Serie-Chnr不能为空"<br>2. 终止流程，不调用 API<br>3. CHECK 按钮恢复可用状态 |          |          |
| 17  | CHECK-记录存在且已激活     | 1. 输入存在的 Serie-Chnr（ACT=Y）<br>2. 点击 CHECK 按钮<br>3. API 返回成功<br>4. 确认结果 | Serie-Chnr 存在且已激活 | 1. 调用 API（GET `/api/ud16/check`）<br>2. 显示消息："记录存在且已激活"                       |          |          |
| 18  | CHECK-记录存在但未激活     | 1. 输入存在的 Serie-Chnr（ACT=N）<br>2. 点击 CHECK 按钮<br>3. API 返回警告<br>4. 确认结果 | Serie-Chnr 存在但未激活 | 1. 显示警告消息："AFTER DEF CHANGE IS NOT ACTIVATED"<br>2. 类型为 Warning 级别                |          |          |
| 19  | CHECK-记录不存在           | 1. 输入不存在的 Serie-Chnr<br>2. 点击 CHECK 按钮<br>3. API 返回 404<br>4. 确认结果        | Serie-Chnr 不存在       | 1. 显示提示信息："记录不存在"                                                                 |          |          |
| 20  | CHECK-失败（API 返回 500） | 1. 输入有效值<br>2. 点击 CHECK 按钮<br>3. API 返回 500<br>4. 确认结果                     | API 返回 500            | 1. 显示错误消息："系统错误，请稍后重试"<br>2. CHECK 按钮恢复可用状态                          |          |          |
| 21  | CHECK-加载中按钮禁用       | 1. 输入有效值<br>2. 点击 CHECK 按钮<br>3. 在 API 响应前确认按钮状态                       | 有效值                  | 1. CHECK 按钮在加载期间禁用（disabled）<br>2. 防止重复提交                                    |          |          |

### 输入字符控制

| No  | 测试对象                    | 操作步骤                                                       | 设定值               | 预期结果                                                                   | 确认结果 | 确认时间 |
| :-: | :-------------------------- | :------------------------------------------------------------- | :------------------- | :------------------------------------------------------------------------- | :------- | :------- |
| 22  | Serie-Chnr-最大长度 15 字符 | 1. 在 Serie-Chnr 输入框中输入 16 个以上字符<br>2. 确认输入结果 | 输入 16 个以上字符   | 1. 输入框最多只能输入 15 个字符（maxLength=15）<br>2. 超出部分无法输入     |          |          |
| 23  | Desc-最大长度 4000 字符     | 1. 在 Desc 输入框中输入 4001 个以上字符<br>2. 确认输入结果     | 输入 4001 个以上字符 | 1. 输入框最多只能输入 4000 个字符（maxLength=4000）<br>2. 超出部分无法输入 |          |          |

### 异常处理

| No  | 测试对象                | 操作步骤                                                             | 设定值 | 预期结果                                                                          | 确认结果 | 确认时间 |
| :-: | :---------------------- | :------------------------------------------------------------------- | :----- | :-------------------------------------------------------------------------------- | :------- | :------- |
| 24  | 异常处理-API 超时       | 1. 模拟 API 响应超时<br>2. 执行 ADD/DELETE/CHECK 操作<br>3. 确认结果 | 无     | 1. 超时后显示错误消息："请求超时，请检查网络连接后重试"<br>2. 按钮恢复可用状态    |          |          |
| 25  | 异常处理-网络连接失败   | 1. 模拟网络断开<br>2. 执行 ADD/DELETE/CHECK 操作<br>3. 确认结果      | 无     | 1. 显示错误消息："网络连接失败，请检查网络设置"<br>2. 按钮恢复可用状态            |          |          |
| 26  | 异常处理-数据库连接异常 | 1. 执行 ADD/DELETE/CHECK 操作时后端数据库异常<br>2. 确认结果         | 无     | 1. API 返回 500<br>2. 显示错误消息："系统错误，请稍后重试"<br>3. 按钮恢复可用状态 |          |          |
| 27  | 异常处理-用户未登录     | 1. 清除登录状态<br>2. 访问页面<br>3. 确认结果                        | 无     | 1. 检测到未登录状态<br>2. 跳转到登录页面                                          |          |          |

### UI 交互

| No  | 测试对象          | 操作步骤                                            | 设定值 | 预期结果                                                                                                        | 确认结果 | 确认时间 |
| :-: | :---------------- | :-------------------------------------------------- | :----- | :-------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 28  | 错误消息-显示样式 | 1. 触发任意错误<br>2. 确认错误消息样式              | 无     | 1. 错误消息显示红色（#ff4d4f）<br>2. 默认隐藏（内容为空时不占位或高度为 0）                                     |          |          |
| 29  | 成功消息-显示样式 | 1. 执行成功 ADD/DELETE 操作<br>2. 确认成功消息样式  | 无     | 1. 成功消息显示绿色（#52c41a）                                                                                  |          |          |
| 30  | 页面刷新          | 1. 输入部分值后<br>2. 刷新页面（F5）<br>3. 确认画面 | 无     | 1. 页面重新加载<br>2. Serie-Chnr 和 Desc 输入框被清空<br>3. 无错误消息和成功消息显示<br>4. 所有按钮恢复可用状态 |          |          |

### 安全性

| No  | 测试对象               | 操作步骤                                                                    | 设定值 | 预期结果                                              | 确认结果 | 确认时间 |
| :-: | :--------------------- | :-------------------------------------------------------------------------- | :----- | :---------------------------------------------------- | :------- | :------- |
| 31  | 安全性-API 请求协议    | 1. 打开浏览器开发者工具<br>2. 执行 ADD/DELETE/CHECK 操作<br>3. 检查网络请求 | 无     | 1. API 请求通过 HTTPS 发送<br>2. 请求中不包含敏感信息 |          |          |
| 32  | 安全性-权限控制        | 1. 以无权限用户登录<br>2. 执行 ADD/DELETE 操作<br>3. 确认结果               | 无     | 1. 仅授权用户可执行增删操作<br>2. 未授权用户无法操作  |          |          |
| 33  | 安全性-DELETE 操作确认 | 1. 点击 DELETE 按钮<br>2. 确认弹出确认对话框                                | 无     | 1. DELETE 操作前弹出确认对话框<br>2. 防止误删重要数据 |          |          |

---
