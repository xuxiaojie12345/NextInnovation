# HDoc User Administration 模块 (UD17) 测试式样书

| 文档编号     | TEST-UD17-001             | 版本号     | v1.0       |
| :----------- | :------------------------ | :--------- | :--------- |
| **模块名称** | HDoc用户管理 (User Admin) | **作成日** | 2026-07-01 |
| **作成者**   | Lingma Assistant          | **状态**   | 正式稿     |

---

## 测试用例明细

### 画面初始化

| No  | 测试对象            | 操作步骤                               | 设定值 | 预想结果                                                                                                                                                                                                             | 确认结果 | 确认时间 |
| :-: | :------------------ | :------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
|  1  | 画面初始化-正常表示 | 1. 访问 UD17 页面<br>2. 确认画面表示   | 无     | 1. 显示 Header<br>2. 显示"HDoc User Admin"标题<br>3. UserID 输入框为空<br>4. User Name 标签为空<br>5. 所有权限复选框为未选中<br>6. Market（Standard User,Adaptation user） 下拉列表默认显示"-EU",其他显示API取得数据 |          |          |
|  2  | 画面初始化-控件状态 | 1. 访问页面<br>2. 确认各控件的初期状态 | 无     | 1. UserID 输入框可输入<br>2. USER INFO 按钮可用<br>3. Update Role 按钮可用<br>4. Delete Role 按钮可用<br>5. 所有权限复选框可操作<br>6. Market 下拉列表可操作                                                         |          |          |

### 空值校验

| No  | 测试对象               | 操作步骤                                             | 设定值 | 预想结果                                                                 | 确认结果 | 确认时间 |
| :-: | :--------------------- | :--------------------------------------------------- | :----- | :----------------------------------------------------------------------- | :------- | :------- |
|  3  | USER INFO-UserID为空   | 1. UserID 为空<br>2. 点击 USER INFO<br>3. 确认结果   | 无     | 1. 显示错误消息："请输入用户 ID"<br>2. 不调用 API<br>3. 焦点停留在输入框 |          |          |
|  4  | Update Role-UserID为空 | 1. UserID 为空<br>2. 点击 Update Role<br>3. 确认结果 | 无     | 1. 显示错误消息："请输入用户 ID"<br>2. 不调用 API                        |          |          |
|  5  | Delete Role-UserID为空 | 1. UserID 为空<br>2. 点击 Delete Role<br>3. 确认结果 | 无     | 1. 显示错误消息："请输入用户 ID"<br>2. 不调用 API<br>3. 不弹出确认对话框 |          |          |
|  6  | 空值校验-全角空格      | 1. 输入全角空格<br>2. 点击任意按钮<br>3. 确认结果    | 空格   | 1. 显示错误消息："请输入用户 ID"<br>2. 不调用 API                        |          |          |

### USER INFO 功能

| No  | 测试对象                  | 操作步骤                                                                          | 设定值            | 预想结果                                                                                                                                                             | 确认结果 | 确认时间 |
| :-: | :------------------------ | :-------------------------------------------------------------------------------- | :---------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
|  7  | USER INFO-用户存在        | 1. 输入有效 UserID<br>2. 点击 USER INFO<br>3. 确认结果                            | UserID: `v0c6900` | 1. 调用 GET API（/ud17userinfo）<br>2. 显示 User Name："John Doe"<br>3. Standard User 和 Rule Admin 复选框被勾选<br>4. Market 下拉列表显示"-EU"<br>5. 不显示错误消息 |          |          |
|  8  | USER INFO-用户不存在(404) | 1. 输入不存在的 UserID<br>2. 点击 USER INFO<br>3. 确认结果                        | UserID: `XXXX`    | 1. API 返回 404<br>2. 显示错误消息："We didn't recognize the userid you entered. Please try again."<br>3. User Name 标签被清空<br>4. 所有权限复选框被取消勾选        |          |          |
|  9  | USER INFO-多种权限组合    | 1. 模拟 API 返回不同 roles 组合<br>2. 输入 UserID 并点击 USER INFO<br>3. 确认结果 | UserID: `v0c6900` | 1. API 返回 roles 列表<br>2. 仅 roles 列表中包含的权限复选框被勾选<br>3. 其他复选框保持未选中                                                                        |          |          |
| 10  | USER INFO-API失败         | 1. 模拟 API 返回错误<br>2. 输入有效 UserID<br>3. 点击 USER INFO<br>4. 确认结果    | UserID: `v0c6900` | 1. 显示错误消息<br>2. 不更新 User Name 和权限复选框                                                                                                                  |          |          |

### Update Role 功能

| No  | 测试对象                                           | 操作步骤                                                                            | 设定值            | 预想结果                                                                                               | 确认结果 | 确认时间 |
| :-: | :------------------------------------------------- | :---------------------------------------------------------------------------------- | :---------------- | :----------------------------------------------------------------------------------------------------- | :------- | :------- |
| 11  | Update Role-正常更新                               | 1. 查询用户后修改权限勾选<br>2. 点击 Update Role<br>3. 确认结果                     | UserID: `v0c6900` | 1. 调用 PUT API（/ud17updaterole）<br>2. 显示成功消息："权限更新成功"<br>3. 权限复选框保持更新后的状态 |          |          |
| 12  | Update Role-部分更新（Template Admin，Rule Admin） | 1. 查询用户后取消部分已选权限<br>2. 点击 Update Role<br>3. 确认结果                 | UserID: `v0c6900` | 1. 调用 PUT API<br>2. 显示成功消息<br>3. 被取消的权限复选框保持未选中<br>4. 其他权限复选框保持原状态   |          |          |
| 13  | Update Role-无权限变更(403)                        | 1. 查询用户后修改权限<br>2. 模拟 API 返回 403<br>3. 点击 Update Role<br>4. 确认结果 | UserID: `v0c6900` | 1. 显示错误消息："您没有权限执行此操作"<br>2. 权限复选框恢复为修改前的状态                             |          |          |
| 14  | Update Role-API失败                                | 1. 模拟 API 返回错误<br>2. 查询用户后修改权限<br>3. 点击 Update Role<br>4. 确认结果 | UserID: `v0c6900` | 1. 显示错误消息<br>2. 权限复选框保持修改后的状态（前端不回滚）                                         |          |          |
| 15  | Update Role-Market一起更新                         | 1. 查询用户后修改 Market 下拉<br>2. 同时修改权限勾选<br>3. 点击 Update Role         | UserID: `v0c6900` | 1. 调用 PUT API 携带 roles 和 market 参数<br>2. 显示成功消息                                           |          |          |

### Delete Role 功能

| No  | 测试对象             | 操作步骤                                                                              | 设定值            | 预想结果                                                                                                                             | 确认结果 | 确认时间 |
| :-: | :------------------- | :------------------------------------------------------------------------------------ | :---------------- | :----------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 16  | Delete Role-确认取消 | 1. 查询用户后点击 Delete Role<br>2. 确认对话框取消<br>3. 确认结果                     | UserID: `v0c6900` | 1. 弹出确认对话框："确定要删除该用户的所有权限吗？"<br>2. 取消后流程终止<br>3. 不调用 API<br>4. 权限复选框状态不变                   |          |          |
| 17  | Delete Role-正常删除 | 1. 查询用户后点击 Delete Role<br>2. 确认对话框确定<br>3. 确认结果                     | UserID: `v0c6900` | 1. 弹出确认对话框<br>2. 确定后调用 DELETE API（/ud17deleterole）<br>3. 显示成功消息："权限已清除"<br>4. 所有权限复选框被重置为未选中 |          |          |
| 18  | Delete Role-API失败  | 1. 查询用户后点击 Delete Role<br>2. 确认对话框确定<br>3. 模拟 API 返回错误<br>4. 确认 | UserID: `v0c6900` | 1. 显示错误消息<br>2. 权限复选框保持删除前的状态                                                                                     |          |          |

### UI交互

| No  | 测试对象                  | 操作步骤                                                                | 设定值            | 预想结果                                                                                                                         | 确认结果 | 确认时间 |
| :-: | :------------------------ | :---------------------------------------------------------------------- | :---------------- | :------------------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 19  | 操作中按钮禁用            | 1. 输入有效 UserID<br>2. 点击 USER INFO<br>3. 在 API 响应前检查按钮状态 | UserID: `v0c6900` | 1. USER INFO 按钮禁用<br>2. Update Role 按钮禁用<br>3. Delete Role 按钮禁用<br>4. UserID 输入框禁用<br>5. API 响应后按钮恢复可用 |          |          |
| 20  | 操作中防止重复提交        | 1. 输入有效 UserID<br>2. 点击 USER INFO<br>3. 在 API 响应前再次点击     | UserID: `v0c6900` | 1. 第二次点击无效（按钮已禁用）<br>2. 只发起一次 API 调用                                                                        |          |          |
| 21  | 消息清空-新操作清除旧消息 | 1. 触发空值错误<br>2. 确认错误消息显示<br>3. 输入有效值后再执行操作     | UserID: `v0c6900` | 1. 第一次操作显示错误消息<br>2. 第二次操作时前一条消息被清除<br>3. 显示新操作对应的消息                                          |          |          |
| 22  | USER INFO后权限修改再查询 | 1. 查询用户后修改部分权限勾选<br>2. 再次点击 USER INFO<br>3. 确认结果   | UserID: `v0c6900` | 1. 再次查询后，权限复选框重置为 API 返回的最新 roles<br>2. 之前的手动修改被覆盖<br>3. Market 重置为 API 返回的值                 |          |          |

### 异常处理

| No  | 测试对象                 | 操作步骤                                                                            | 设定值            | 预想结果                                                                                        | 确认结果 | 确认时间 |
| :-: | :----------------------- | :---------------------------------------------------------------------------------- | :---------------- | :---------------------------------------------------------------------------------------------- | :------- | :------- |
| 23  | 异常处理-网络断开        | 1. 模拟网络断开<br>2. 输入有效 UserID<br>3. 执行任意操作<br>4. 确认结果             | UserID: `v0c6900` | 1. 显示错误消息："网络连接失败，请检查网络设置"<br>2. 按钮恢复可用状态                          |          |          |
| 24  | 异常处理-API超时         | 1. 模拟 API 响应超时<br>2. 输入有效 UserID<br>3. 执行操作<br>4. 等待超时<br>5. 确认 | UserID: `v0c6900` | 1. 超时后显示错误消息："网络连接失败，请检查网络设置" 或类似提示<br>2. 按钮恢复可用状态         |          |          |
| 25  | 异常处理-服务器500错误   | 1. 模拟 API 返回 500 错误<br>2. 输入有效 UserID<br>3. 执行操作<br>4. 确认结果       | UserID: `v0c6900` | 1. 显示错误消息："系统繁忙，请稍后再试"<br>2. 按钮恢复可用状态                                  |          |          |
| 26  | 异常处理-Saviynt同步失败 | 1. 模拟 API 返回 Saviynt 错误<br>2. 输入有效 UserID<br>3. 点击 USER INFO<br>4. 确认 | UserID: `v0c6900` | 1. 显示错误消息："无法获取用户主数据，请稍后重试"<br>2. User Name 不更新<br>3. 权限复选框不更新 |          |          |

---
